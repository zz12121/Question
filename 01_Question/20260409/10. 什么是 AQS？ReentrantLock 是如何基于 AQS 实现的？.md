## 先说结论

**AQS = AbstractQueuedSynchronizer，是 JUC 包的核心框架，提供了一个基于 FIFO 等待队列的阻塞锁和同步器的基础实现。ReentrantLock、CountDownLatch、Semaphore 都是基于 AQS 实现的。**

```
核心思想：
用一个 int 类型的 state 表示同步状态
用一个 FIFO 双向链表管理等待线程
子类只需要实现 state 的获取和释放逻辑
```

---

## 🔥 核心概念

### AQS 解决什么问题？

**场景：多个线程竞争同一资源**

```
没有 AQS 时，每个同步器都要自己实现：
- 线程排队
- 阻塞和唤醒
- 可重入
- 公平/非公平
- 中断处理

代码重复，容易出错！
```

**有了 AQS：**

```
抽象公共逻辑：
- 队列管理
- 线程阻塞/唤醒（LockSupport）
- CAS 操作

子类只需实现：
- tryAcquire()：尝试获取资源
- tryRelease()：尝试释放资源
```

---

## 🔥 AQS 的核心结构

### 1. 同步状态 state

```java
public abstract class AbstractQueuedSynchronizer 
    extends AbstractOwnableSynchronizer {
    
    // 核心：volatile 保证可见性
    private volatile int state;
    
    // CAS 操作 state
    protected final boolean compareAndSetState(int expect, int update) {
        return unsafe.compareAndSwapInt(this, stateOffset, expect, update);
    }
    
    // 子类需要实现的方法
    protected abstract boolean tryAcquire(int arg);
    protected abstract boolean tryRelease(int arg);
    protected abstract int tryAcquireShared(int arg);
    protected abstract boolean tryReleaseShared(int arg);
}
```

**state 的含义由子类定义：**

| 同步器 | state 含义 |
|--------|-----------|
| ReentrantLock | 0=未锁定，>0=重入次数 |
| CountDownLatch | 剩余计数 |
| Semaphore | 剩余许可数 |
| ReentrantReadWriteLock | 高16位=读锁，低16位=写锁 |

---

### 2. FIFO 等待队列（CLH 变体）

```java
// 队列头尾节点（延迟初始化）
private transient volatile Node head;
private transient volatile Node tail;

// 节点定义
static final class Node {
    // 共享模式
    static final Node SHARED = new Node();
    // 独占模式
    static final Node EXCLUSIVE = null;
    
    // 等待状态
    volatile int waitStatus;
    static final int CANCELLED =  1;  // 取消
    static final int SIGNAL    = -1;  // 后继节点需要唤醒
    static final int CONDITION = -2;  // 在 Condition 队列中
    static final int PROPAGATE = -3;  // 共享模式传播
    
    volatile Node prev;       // 前驱
    volatile Node next;       // 后继
    volatile Thread thread;   // 绑定的线程
    
    Node nextWaiter;          // 共享/独占模式或 Condition 队列
}
```

**队列结构：**

```
      head                         tail
        │                           │
        ▼                           ▼
    ┌──────┐    ┌──────┐    ┌──────┐
    │ Node │───→│ Node │───→│ Node │
    │ ws=0 │←───│ ws=-1│←───│ ws=-1│
    └──────┘    └──────┘    └──────┘
       ↑
    持有锁的线程（不在队列中，或 dummy 节点）
```

---

### 3. 独占式获取与释放流程

**获取锁（acquire）：**

```java
public final void acquire(int arg) {
    // 1. 尝试获取（子类实现）
    // 2. 失败则加入队列，自旋或阻塞
    if (!tryAcquire(arg) &&
        acquireQueued(addWaiter(Node.EXCLUSIVE), arg)) {
        // 3. 如果被中断过，补中断标志
        selfInterrupt();
    }
}
```

**详细流程：**

```java
// 1. 加入队列尾部
private Node addWaiter(Node mode) {
    Node node = new Node(Thread.currentThread(), mode);
    Node pred = tail;
    if (pred != null) {
        node.prev = pred;
        // CAS 设置尾节点
        if (compareAndSetTail(pred, node)) {
            pred.next = node;
            return node;
        }
    }
    // 队列未初始化或 CAS 失败，进入完整入队
    enq(node);
    return node;
}

// 2. 在队列中等待获取
final boolean acquireQueued(final Node node, int arg) {
    boolean failed = true;
    try {
        boolean interrupted = false;
        for (;;) {  // 自旋
            final Node p = node.predecessor();
            
            // 前驱是头节点，尝试获取锁
            if (p == head && tryAcquire(arg)) {
                setHead(node);  // 获取成功，成为头节点
                p.next = null;  // help GC
                failed = false;
                return interrupted;
            }
            
            // 判断是否需要阻塞
            if (shouldParkAfterFailedAcquire(p, node) &&
                parkAndCheckInterrupt()) {
                interrupted = true;  // 被中断了
            }
        }
    } finally {
        if (failed) cancelAcquire(node);
    }
}

// 3. 阻塞线程
private final boolean parkAndCheckInterrupt() {
    LockSupport.park(this);  // 真正阻塞！
    return Thread.interrupted();
}
```

**释放锁（release）：**

```java
public final boolean release(int arg) {
    // 1. 尝试释放（子类实现）
    if (tryRelease(arg)) {
        Node h = head;
        // 2. 有后继节点且需要唤醒
        if (h != null && h.waitStatus != 0) {
            unparkSuccessor(h);  // 唤醒后继
        }
        return true;
    }
    return false;
}

private void unparkSuccessor(Node node) {
    int ws = node.waitStatus;
    if (ws < 0) compareAndSetWaitStatus(node, ws, 0);
    
    Node s = node.next;
    // 找下一个有效的后继节点
    if (s == null || s.waitStatus > 0) {
        s = null;
        for (Node t = tail; t != null && t != node; t = t.prev)
            if (t.waitStatus <= 0) s = t;
    }
    if (s != null)
        LockSupport.unpark(s.thread);  // 唤醒！
}
```

---

## 🔥 ReentrantLock 如何基于 AQS 实现

### 类结构

```java
public class ReentrantLock implements Lock, java.io.Serializable {
    private final Sync sync;  // 内部 AQS 子类
    
    // 抽象同步器
    abstract static class Sync extends AbstractQueuedSynchronizer {
        abstract void lock();
        
        // 非公平尝试获取
        final boolean nonfairTryAcquire(int acquires) {
            final Thread current = Thread.currentThread();
            int c = getState();
            
            if (c == 0) {  // 锁空闲
                if (compareAndSetState(0, acquires)) {
                    setExclusiveOwnerThread(current);
                    return true;
                }
            } else if (current == getExclusiveOwnerThread()) {  // 重入
                int nextc = c + acquires;
                if (nextc < 0) throw new Error("Maximum lock count exceeded");
                setState(nextc);
                return true;
            }
            return false;
        }
        
        // 释放锁
        protected final boolean tryRelease(int releases) {
            int c = getState() - releases;
            if (Thread.currentThread() != getExclusiveOwnerThread())
                throw new IllegalMonitorStateException();
            boolean free = false;
            if (c == 0) {  // 完全释放
                free = true;
                setExclusiveOwnerThread(null);
            }
            setState(c);
            return free;
        }
    }
    
    // 非公平锁
    static final class NonfairSync extends Sync {
        final void lock() {
            // 先 CAS 抢一下，抢不到再排队
            if (compareAndSetState(0, 1))
                setExclusiveOwnerThread(Thread.currentThread());
            else
                acquire(1);  // 进入 AQS 队列
        }
        
        protected final boolean tryAcquire(int acquires) {
            return nonfairTryAcquire(acquires);
        }
    }
    
    // 公平锁
    static final class FairSync extends Sync {
        final void lock() {
            acquire(1);  // 直接排队，不抢
        }
        
        protected final boolean tryAcquire(int acquires) {
            final Thread current = Thread.currentThread();
            int c = getState();
            if (c == 0) {
                // 关键区别：检查队列里有没有等待的线程
                if (!hasQueuedPredecessors() &&  // 没有前驱才尝试
                    compareAndSetState(0, acquires)) {
                    setExclusiveOwnerThread(current);
                    return true;
                }
            } else if (current == getExclusiveOwnerThread()) {
                int nextc = c + acquires;
                if (nextc < 0) throw new Error("Maximum lock count exceeded");
                setState(nextc);
                return true;
            }
            return false;
        }
    }
}
```

---

## 🔥 扩展追问 1：公平锁 vs 非公平锁的实现差异

```java
// 非公平锁：先抢一下，抢不到再排队
final void lock() {
    if (compareAndSetState(0, 1))  // 直接 CAS 抢！
        setExclusiveOwnerThread(Thread.currentThread());
    else
        acquire(1);  // 抢不到，走正常流程
}

// 公平锁：直接排队
final void lock() {
    acquire(1);  // 不抢，老老实实排队
}

// tryAcquire 的区别
// 公平锁多了 hasQueuedPredecessors() 检查
protected final boolean tryAcquire(int acquires) {
    if (c == 0) {
        if (!hasQueuedPredecessors() &&  // 关键区别！
            compareAndSetState(0, acquires)) {
            // ...
        }
    }
}
```

**hasQueuedPredecessors：**

```java
public final boolean hasQueuedPredecessors() {
    Node t = tail;
    Node h = head;
    Node s;
    // 队列有节点，且头节点的后继不是当前线程
    return h != t &&
        ((s = h.next) == null || s.thread != Thread.currentThread());
}
```

---

## 🔥 扩展追问 2：可重入如何实现？

```java
// 获取时检查是否是当前线程
final boolean nonfairTryAcquire(int acquires) {
    final Thread current = Thread.currentThread();
    int c = getState();
    
    if (c == 0) {
        // 尝试获取
    } 
    // 关键：是当前线程，state + 1
    else if (current == getExclusiveOwnerThread()) {
        int nextc = c + acquires;  // 重入计数 +1
        setState(nextc);
        return true;
    }
    return false;
}

// 释放时 state - 1，到 0 才真正释放
protected final boolean tryRelease(int releases) {
    int c = getState() - releases;  // 计数 -1
    if (c == 0) {  // 完全释放
        setExclusiveOwnerThread(null);
        return true;  // 返回 true 唤醒后继
    }
    setState(c);
    return false;  // 还没完全释放，不唤醒
}
```

---

## 🔥 扩展追问 3：AQS 的 Condition 实现

```java
public class ConditionObject implements Condition, java.io.Serializable {
    // Condition 队列（单向链表）
    private transient Node firstWaiter;
    private transient Node lastWaiter;
    
    // await：释放锁，加入 Condition 队列，阻塞
    public final void await() throws InterruptedException {
        if (Thread.interrupted()) throw new InterruptedException();
        
        Node node = addConditionWaiter();  // 加入 Condition 队列
        
        int savedState = fullyRelease(node);  // 完全释放锁
        
        int interruptMode = 0;
        while (!isOnSyncQueue(node)) {  // 不在同步队列就阻塞
            LockSupport.park(this);
            if ((interruptMode = checkInterruptWhileWaiting(node)) != 0)
                break;
        }
        
        // 被 signal 后，重新竞争锁
        if (acquireQueued(node, savedState) && interruptMode != THROW_IE)
            interruptMode = REINTERRUPT;
        
        // 清理取消的节点
        if (node.nextWaiter != null) unlinkCancelledWaiters();
    }
    
    // signal：从 Condition 队列移到同步队列
    public final void signal() {
        if (!isHeldExclusively()) throw new IllegalMonitorStateException();
        Node first = firstWaiter;
        if (first != null) doSignal(first);
    }
    
    private void doSignal(Node first) {
        do {
            if ((firstWaiter = first.nextWaiter) == null)
                lastWaiter = null;
            first.nextWaiter = null;
        } while (!transferForSignal(first) &&  // 转移到同步队列
                 (first = firstWaiter) != null);
    }
}
```

**两个队列的关系：**

```
AQS 同步队列（双向，竞争锁）：
head ↔ Node ↔ Node ↔ Node ↔ tail

Condition 等待队列（单向，等条件）：
firstWaiter → Node → Node → Node → lastWaiter

await：从同步队列移除 → 加入 Condition 队列
signal：从 Condition 队列移除 → 加入同步队列尾部
```

---

## 🔥 扩展追问 4：共享模式（Semaphore/CountDownLatch）

```java
// 共享获取
public final void acquireShared(int arg) {
    if (tryAcquireShared(arg) < 0)  // 返回值 < 0 表示失败
        doAcquireShared(arg);
}

// Semaphore 的实现
static final class NonfairSync extends Sync {
    protected int tryAcquireShared(int acquires) {
        for (;;) {
            int available = getState();
            int remaining = available - acquires;
            if (remaining < 0 ||
                compareAndSetState(available, remaining))
                return remaining;  // >=0 成功，<0 失败
        }
    }
}

// 共享释放（会唤醒多个后继）
public final boolean releaseShared(int arg) {
    if (tryReleaseShared(arg)) {
        doReleaseShared();  // 唤醒后继，可能级联传播
        return true;
    }
    return false;
}
```

---

## 🔥 扩展追问 5：AQS 的设计亮点

| 设计 | 说明 |
|------|------|
| 模板方法模式 | 父类定义框架，子类实现细节 |
| CLH 变体队列 | FIFO 保证公平，支持取消 |
| CAS + 自旋 | 竞争不激烈时避免阻塞 |
| LockSupport | 真正阻塞/唤醒线程 |
| 独占+共享 | 一套框架支持多种场景 |
| Condition | 支持多条件等待 |

---

## 一句话收尾

> "AQS 是 JUC 包的核心框架，用一个 volatile int state 表示同步状态，用一个 FIFO 双向队列管理等待线程。ReentrantLock 通过继承 AQS，实现 tryAcquire/tryRelease 方法，用 state 表示锁的重入次数，通过 CAS 操作 state 来实现获取和释放。公平锁和非公平锁的区别在于是否先尝试 CAS 抢锁，以及是否检查队列前驱。Condition 通过另一个单向链表实现条件等待。"
