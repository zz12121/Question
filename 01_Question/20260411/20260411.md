# 理论综合题 - 第3天（61-90）

## Java 基础（5题）

61. 什么是 Java 的反射机制？反射的优缺点？在什么场景下使用？

62. Java 8 的 Lambda 表达式是如何实现的？函数式接口是什么？

63. Java 8 的 Stream API 有哪些常用操作？并行流（parallelStream）的原理和注意事项？

64. 什么是 Java 的序列化？如何实现？serialVersionUID 的作用？

65. Java 中的四种引用类型（强、软、弱、虚）分别是什么？使用场景？

---

## Java 并发（6题）

66. 什么是线程安全？不可变对象一定是线程安全的吗？

67. 什么是死锁？死锁的四个必要条件？如何排查和避免死锁？

68. 什么是活锁和饥饿？和死锁有什么区别？

69. `synchronized` 的底层实现原理？Monitor 是什么？

70. `ReentrantReadWriteLock` 的读锁和写锁是什么关系？写锁可以降级为读锁吗？

71. `Exchanger` 是什么？使用场景？

---

## JVM（5题）

72. 什么是直接内存（Direct Memory）？和堆内存有什么区别？

73. 什么是逃逸分析？标量替换？栈上分配？

74. 什么是安全点（Safepoint）？什么是安全区域（Safe Region）？

75. G1 垃圾收集器的 Mixed GC 和 Full GC 有什么区别？什么情况下会触发 Full GC？

76. JVM 调优的常见步骤？如何确定堆内存大小？

---

## MySQL（5题）

77. MySQL 的 InnoDB 引擎中，聚簇索引和非聚簇索引（二级索引）有什么区别？回表是什么？如何避免回表？覆盖索引又是什么？

78. MySQL 的 `ORDER BY` 在什么情况下会使用文件排序（filesort）？什么情况下可以用到索引排序？`GROUP BY` 和 `ORDER BY` 同时出现时怎么优化？

79. MySQL 中 `INSERT` 语句在 InnoDB 内部的执行流程是怎样的？从客户端发送 SQL 到数据落盘，中间经历了哪些步骤？redo log、undo log、binlog 分别什么时候写入？

80. 什么是 MVCC（多版本并发控制）？InnoDB 的 undo log 版本链是怎么组织的？ReadView 的 visible 规则是什么？为什么 RR 级别能解决不可重复读？

81. MySQL 的 `LIMIT 1000000, 10` 这种深分页为什么慢？除了"延迟关联法"，还有什么优化方案？如果业务上必须支持任意页码跳转，怎么设计？

---

## Redis（4题）

82. Redis 做分布式缓存时，如果缓存容量有限（比如只有 4GB），但数据量远超这个规模，你会用什么淘汰策略？LRU、LFU、Random 各自的适用场景是什么？Redis 的近似 LRU 和精确 LRU 有什么区别？

83. Redis Cluster 的数据分片原理是什么？16384 个 slot 是怎么分配到各个节点的？当某个节点宕机时，集群是如何感知并完成故障转移的？和 Sentinel 方案的本质区别是什么？

84. 你用过 Redis 的 `SCAN` 命令吗？它和 `KEYS *` 有什么区别？`SCAN` 的 cursor 是怎么实现的？为什么说 `SCAN` 可能会返回重复元素或漏掉元素？

85. Redis 的 Pub/Sub（发布订阅）功能在实际项目中用过多吗？它有什么局限性？如果要做可靠的消息通知（确保消息不丢失），Redis 的 Stream 数据类型可以怎么实现？

---

## Spring / Spring Boot（3题）

86. Spring 中 `@Transactional` 注解的事务失效场景有哪些？（至少说出 6 种）每种情况分别说明原因和解决方案。

87. Spring MVC 的请求处理完整流程是怎样的？一个 HTTP 请求从到达 DispatcherServlet 到返回响应，中间经过了哪些组件？HandlerAdapter 的作用是什么？

88. 你了解过 Spring 的事件机制（ApplicationEvent）吗？它是同步还是异步的？如果一个事件有多个监听器，它们的执行顺序怎么控制？事务监听器 `@TransactionalEventListener` 怎么用？

---

## 分布式（2题）

89. 在微服务架构中，服务之间的调用链路变长后，如何在全链路上传递用户身份信息（如 userId、traceId）？如果使用了线程池异步处理，上下文会丢失怎么办？

90. 什么是 ID 幂等性设计？在支付回调、消息消费、接口重复提交这些场景下，如何设计一套通用的幂等方案？幂等表怎么建？TTL 怎么设置？
