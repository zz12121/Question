# 理论综合题 - 补充（A211-A235）

> 以下为对前7天630题中原有领域内部遗漏点的补充，编号从211起，严格只涉及 Java基础、并发、JVM、MySQL、Redis、Spring、分布式这七大原有领域。

---

## Java 基础（4题）

211. HashMap 在 JDK 8 中链表转红黑树的阈值为什么是 8？为什么退化阈值是 6 而不是 8？（泊松分布概率计算：8 时转红黑树和 6 时退化是为了避免频繁转换带来的性能抖动）TreeMap 和 HashMap 的核心区别是什么？TreeMap 的红黑树增删查时间复杂度是多少？HashMap 中红黑树节点的查找是如何利用Comparable或Comparator的？

212. Java 的自动装箱（Autoboxing）和拆箱（Unboxing）在什么场景下会发生？`Integer a = 127; Integer b = 127; a == b` 结果是什么？`Integer a = 128; Integer b = 128; a == b` 呢？为什么 Integer 缓存池范围是 -128 到 127？这个范围可以修改吗？自动拆箱导致的 NPE 在什么场景下容易踩坑？（比如 `Integer x = null; int y = x;`）

213. Java 运行时数据区（Runtime Data Area）的完整划分是怎样的？程序计数器、虚拟机栈、本地方法栈、堆、方法区各自存储什么数据？线程共享和线程私有的区域分别有哪些？一个方法的栈帧（Stack Frame）包含哪些部分？（局部变量表、操作数栈、动态链接、方法返回地址）递归调用 100 层会栈溢出吗？-Xss 参数控制的是什么？

214. LinkedHashMap 和 HashMap 的核心区别是什么？`accessOrder=true` 时如何实现 LRU 缓存？HashSet 的底层实现是什么？为什么说 HashSet 就是一个"只有 key 的 HashMap"？TreeSet 的底层实现是什么？它和 HashSet 的性能差异在哪里？什么时候该用 TreeSet 而不是 HashSet？

---

## Java 并发（4题）

215. volatile 关键字的底层实现原理是什么？JVM 层面是怎么保证可见性和有序性的？（内存屏障：StoreStore、StoreLoad、LoadLoad、LoadStore 四种屏障）`lock` 前缀指令在 x86 架构上做了什么？volatile 能保证 `i++` 的原子性吗？什么场景下 volatile 比 synchronized 更合适？（一写多读的状态标志、DCL 双重检查锁的单例）volatile 的写-读建立了 happens-before 关系，具体是哪条规则？

216. synchronized 在 JDK 6 之后的锁升级过程是怎样的？偏向锁（Biased Locking）的原理是什么？为什么 JDK 15 之后默认禁用了偏向锁？（锁撤销开销大、维护复杂度高）轻量级锁的自适应自旋是怎么工作的？`-XX:PreBlockSpin` 参数的作用是什么？重量级锁的 Monitor 对象中 WaitSet 和 EntryList 分别存什么？synchronized 锁升级是不可逆的吗？

217. Java 内存模型（JMM）的主内存和工作内存分别对应什么？（不直接对应物理内存，是一种抽象规范）JMM 的 8 条 Happens-Before 规则分别是什么？除了 volatile 和 synchronized，还有哪些方式能建立 happens-before？（Thread.start() / Thread.join() / 并发工具类的操作等）为什么有了 happens-before 规则，编译器仍然可以做指令重排序？（as-if-serial 语义）

218. CompletableFuture 的 exceptionally()、handle()、whenComplete() 三个异常处理方法有什么区别？exceptionally 只能处理异常返回默认值，handle 能同时处理正常值和异常值，whenComplete 没有返回值只能做副作用操作——对吗？CompletableFuture 链式调用中，如果中间某个环节抛了异常，后续环节还会执行吗？如何实现"某个 Future 超时后取默认值"而不阻塞整体？

---

## JVM（3题）

219. 什么是 JVM 的 Deoptimization（去优化）？什么情况下 JIT 编译后的代码会被"反优化"回解释执行？（类型不兼容 / 分支预测失败 / 类层级变更）去优化对性能的影响有多大？如何用 `-XX:+PrintCompilation` 观察去优化事件？C1 编译器和 C2 编译器在编译策略上有什么本质区别？（C1 快速编译不做深度优化、C2 做全面优化但编译时间长）OSR（On-Stack Replacement）栈上替换是什么？

220. 什么是 JVM 的 Code Cache？它存储的是什么？（JIT 编译生成的本地机器码）Code Cache 满了会发生什么？（JIT 编译被禁用，退化为解释执行，性能急剧下降）`ReservedCodeCacheSize` 和 `InitialCodeCacheSize` 分别是什么？如何监控 Code Cache 的使用情况？（`-XX:+PrintCodeCache` / JMC）线程内联深度受什么参数控制？`-XX:MaxInlineSize` 和 `-XX:FreqInlineSize` 的区别？

221. Java 9 引入的 Compact Strings（紧凑字符串）是怎么工作的？为什么 `String` 内部从 `char[]` 改成了 `byte[]` 加编码标识符？对内存节省有多大？Latin-1 编码的字符串占用的内存是之前的一半吗？Compact Strings 对 String 的操作性能有影响吗？在什么情况下字符串会被"解压"（从 byte[] 还原为 char[]）？

---

## MySQL（4题）

222. MySQL 的 Generated Column（生成列）和函数索引分别是什么？生成列的 `VIRTUAL` 和 `STORED` 模式有什么区别？函数索引和普通索引有什么不同？什么场景下适合用函数索引？（JSON 字段查询、LOWER(column) 查询）MySQL 8.0 的降序索引（DESC INDEX）有什么用？为什么在 MySQL 5.7 中创建的 DESC INDEX 实际上是 ASC 的？

223. MySQL 的 SHOW ENGINE INNODB STATUS 命令能输出哪些关键信息？`TRANSACTIONS` 段中能看到什么？（当前活跃事务、锁等待链）`BUFFER POOL AND MEMORY` 段中能看到什么？（Buffer Pool 命中率、脏页比例）`FILE I/O` 段中能看到什么？（异步 I/O 线程的工作情况）PURGE 线程的作用是什么？为什么 undo log 的清理速度会影响整体性能？undo log 的 truncate 和 purge 有什么区别？

224. MySQL 的 `EXPLAIN` 输出中，`type` 列的值从好到差的顺序是什么？（system > const > eq_ref > ref > range > index > ALL）`Extra` 列中 `Using index`、`Using index condition`（ICP）、`Using where`、`Using filesort`、`Using temporary` 分别代表什么？`key_len` 列的值是怎么计算的？为什么联合索引中 varchar(50) 的 key_len 可能是 153 而不是 50？（utf8mb4 每个字符最多 4 字节 + 允许 NULL 需要 1 字节）

225. MySQL InnoDB 的 Buffer Pool 预读机制（Read-Ahead）是什么？线性预读（Linear Read-Ahead）和随机预读（Random Read-Ahead）分别在什么场景下触发？`innodb_read_ahead_threshold` 参数的含义是什么？预读会浪费 Buffer Pool 空间吗？预读的页面什么时候会被驱逐？`innodb_random_read_ahead` 默认是关闭的，什么场景下应该开启？

---

## Redis（3题）

226. Redis 的 `EVALSHA` 和 `EVAL` 有什么区别？为什么生产环境推荐用 `EVALSHA`？（EVAL 每次都传输完整脚本，EVALSHA 只传 SHA1 校验和，节省带宽）如果 Redis 重启后脚本缓存丢失，`EVALSHA` 会报什么错？（NOSCRIPT 错误）Redis 7.0 引入的 Redis Function 相比 Lua 脚本有什么优势？（持久化、函数库管理、更安全的执行方式）Lua 脚本的执行是原子的吗？如果 Lua 脚本执行时间过长（超过 `lua-time-limit`）会怎样？

227. Redis 的 RDB 持久化在 fork 子进程时的 `copy-on-write` 机制是怎么工作的？为什么 fork 在 Redis 内存很大时会很慢？（Linux 的 fork 使用 copy-on-write，但需要复制页表）THP（Transparent Huge Pages）为什么会影响 Redis 的 fork 性能？如何关闭 THP？RDB 文件的压缩（`rdbcompression`）对 CPU 和磁盘的影响是什么？AOF 的 `appendfsync everysec` 在极端情况下最多丢失多少数据？

228. Redis 的 ACL（Access Control List）权限控制是怎么工作的？Redis 6.0 之前只有 requirepass 密码认证，为什么引入 ACL？（支持多用户、每个用户可以有不同的命令权限和 key 权限）如何创建一个只能对 `order:*` 前缀的 key 执行 GET 命令的用户？`aclcat` 和 `aclgenpass` 命令的作用是什么？在多租户场景下 ACL 怎么做 key 级别的隔离？

---

## Spring / Spring Boot（4题）

229. Spring 的循环依赖问题是怎么产生的？构造器注入和字段注入在循环依赖场景下有什么不同？Spring 是如何通过"三级缓存"（singletonObjects / earlySingletonObjects / singletonFactories）解决单例 Bean 的循环依赖的？为什么三级缓存需要 ObjectFactory 而不是直接存 earlySingletonObjects？（为了支持 AOP 代理的延迟创建）什么情况下 Spring 无法解决循环依赖？（构造器注入循环依赖、prototype Bean）

230. Spring Boot 的 `@Conditional` 系列注解有哪些？`@ConditionalOnProperty`、`@ConditionalOnClass`、`@ConditionalOnBean`、`@ConditionalOnMissingBean` 分别在什么场景下使用？自动配置类的条件判断通常组合哪些注解？为什么 Starter 中的自动配置类经常用 `@ConditionalOnMissingBean`？（允许用户自定义 Bean 来覆盖自动配置）如何自定义一个 `@Conditional` 条件？

231. SpEL（Spring Expression Language）是什么？`#{}` 和 `${}` 的区别是什么？`${}` 只是属性占位符替换，`#{}` 支持 SpEL 表达式——`@Value("#{systemProperties['java.io.tmpdir']}")` 和 `@Value("${java.io.tmpdir}")` 的区别？SpEL 在 `@Cacheable` 的 `key` 和 `condition` 参数中怎么用？SpEL 的安全风险是什么？（可以执行任意方法，`T(Runtime).getRuntime().exec()`）

232. Spring Boot 的配置文件加载顺序是怎样的？`application.yml`、`application-{profile}.yml`、`bootstrap.yml`、命令行参数、环境变量、`spring.config.import` 的优先级如何排列？如果同时存在 `application.yml` 和 `application.properties`，谁优先生效？如何在不同 profile 下加载不同的配置类？`spring.profiles.active` 和 `spring.profiles.group`（Spring Boot 2.4+）的区别是什么？

---

## 分布式（3题）

233. 分布式系统中的"脑裂"（Split-Brain）问题是什么？在 ZooKeeper 集群中，什么情况下会发生脑裂？（网络分区导致两个独立的 Quorum 各自选出 Leader）ZooKeeper 的 `Zab` 协议是如何通过过半机制避免脑裂的？Redis Cluster 的 Gossip 协议 + 主从复制架构，在发生网络分区时会有脑裂风险吗？分布式共识算法 Paxos、Raft、Zab 的核心区别是什么？

234. 什么是分布式系统中的"惊群效应"（Thundering Herd）？在什么场景下会触发？（大量客户端同时监听同一个 ZooKeeper 节点变化、缓存失效瞬间大量请求同时查询数据库）如何避免惊群？（ZooKeeper 的 sequential ephemeral znode / Redis 的随机过期时间 / 分布式锁的退避重试）"缓存雪崩"和"惊群效应"有什么关系？

235. 分布式事务中的 Saga 模式是什么？和 TCC 模式相比有什么区别？Saga 的"正向操作"和"补偿操作"分别是什么？Saga 有两种实现方式：基于编排（Choreography）和基于协调（Orchestration），分别适合什么场景？如果补偿操作本身也失败了怎么办？（重试 + 人工干预）Saga 模式的"可观测性"怎么保证？（每一步操作的日志 + 全局事务状态表）
