# 理论综合题 - 第4天（91-120）

## Java 基础（5题）

91. 什么是 Java 的动态代理？JDK 动态代理和 CGLIB 动态代理的区别？

92. Java 的 `final` 关键字有哪些用法？修饰类、方法、变量分别有什么效果？`final` 修饰的集合（如 `List<String>`）内容还能改吗？JMM 中 `final` 字段的有何特殊语义？

93. Java 中的 `equals()` 和 `hashCode()` 有什么关系？为什么重写 `equals()` 必须重写 `hashCode()`？

94. Java 中的 `try-catch-finally` 中，如果 `catch` 块中抛出了异常，`finally` 块还会执行吗？如果在 `finally` 块中 `return`，会发生什么？`try-with-resources` 的底层原理是什么？

95. Java 中的 `Comparable` 和 `Comparator` 有什么区别？

---

## Java 并发（6题）

96. 在生产环境中配置线程池，你会怎么确定核心线程数和最大线程数？IO 密集型和 CPU 密集型任务的区别是什么？有没有通用的经验公式？这些公式的局限性在哪里？

97. `ThreadPoolExecutor` 的 `execute()` 方法和 `submit()` 方法有什么区别？`submit()` 返回的 `Future.get()` 时如果任务抛了异常，会怎么处理？如何自定义线程池的线程命名（方便排查问题）？

98. 什么是 `ScheduledThreadPoolExecutor`？和 `Timer` 有什么区别？

99. 什么是 `Phaser`？和 `CyclicBarrier` 有什么区别？

100. 什么是 `TransferQueue`？和 `BlockingQueue` 有什么区别？

101. 什么是 `ExecutorCompletionService`？使用场景？

---

## JVM（5题）

102. 什么是 Java 的类加载过程（加载→链接→初始化）？链接阶段中的"验证、准备、解析"分别做了什么？`<clinit>` 和 `<init>` 方法的区别是什么？类什么时候会被初始化（主动使用的 6 种场景）？

103. 什么是 Tomcat 的类加载机制？为什么打破双亲委派？

104. 什么是 JVM 的编译优化？方法内联、逃逸分析、锁消除分别是什么？

105. 什么是 ZGC 和 Shenandoah？和 G1 有什么区别？

106. 什么是 JVM 的 JIT 编译？热点代码是如何识别的？

---

## MySQL（5题）

107. MySQL 的 `UPDATE ... SET balance = balance - 100 WHERE id = 1` 这条 SQL 在 InnoDB 内部是如何执行的？`Buffer Pool` 中的脏页什么时候被刷盘？`checkpoint` 机制是什么？ sharp checkpoint 和 fuzzy checkpoint 有什么区别？

108. 什么是 MySQL 的 binlog？和 redo log 有什么区别？

109. MySQL 在 RR 隔离级别下，`SELECT * FROM user WHERE age > 18 FOR UPDATE` 这条 SQL 会加什么锁？如果 age 列没有索引，加锁范围会怎样变化？什么是"锁升维"？如何避免？

110. MySQL 的 `INSERT ... ON DUPLICATE KEY UPDATE` 语句的加锁行为是怎样的？如果主键冲突和唯一索引冲突，加锁情况一样吗？在 RR 和 RC 隔离级别下表现有什么不同？

111. 什么是 MySQL 的意向锁（Intention Lock）？有什么作用？

---

## Redis（4题）

112. 什么是 Redis 的 Lua 脚本？有什么优势？如何保证原子性？

113. 什么是 Redis 的 Pipeline？和批量命令有什么区别？

114. Redis 在做消息队列时，如果消费者处理一半挂了，消息就丢了。Redis Stream 的 `XACK` / `XCLAIM` / `XGROUP` 是怎么解决"消息不丢失"+"消息不重复消费"的？它的 `PEL`（Pending Entry List）是什么？

115. 什么是 Redis 的 Stream？和 Kafka 有什么区别？

---

## Spring / Spring Boot（3题）

116. 什么是 Spring 的 BeanPostProcessor？使用场景？

117. 什么是 Spring 的 BeanFactoryPostProcessor？和 BeanPostProcessor 有什么区别？

118. 什么是 Spring Boot Actuator？可以用来做什么？

---

## 分布式（2题）

119. 在分布式系统中，如何保证全局配置的一致性？如果用 ZooKeeper 做配置中心，Watch 机制是什么？Zab 协议和 Raft 协议的核心区别是什么？Nacos 的 AP 模式（Distro）和 CP 模式（Raft）分别适用于什么场景？

120. 什么是分布式链路追踪？SkyWalking、Zipkin、Jaeger 分别是什么？
