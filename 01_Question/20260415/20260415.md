# 理论综合题 - 第7天（181-210）

## Java 基础（5题）

181. Java 中的 `Record` 类（JDK 14+ 预览，JDK 16 正式）是什么？和 `Lombok @Data` 有什么本质区别？Record 的构造方法能不能自定义？能不能继承其他类（extends）？能不能用 implements 实现接口？Record 的字段能不能用 `volatile` 修饰？反编译后的 Record 类结构是什么样的？

182. 什么是 Java 的 `sealed class`（密封类，JDK 15 预览，JDK 17 正式）？`permits` 子句的作用是什么？`sealed`、`non-sealed`、`final` 三者的关系是什么？密封类在 `switch` 模式匹配（JDK 21）中如何实现穷举检查？这对"代数数据类型 ADT"的建模有什么帮助？

183. 什么是 Java 的 `Foreign Function & Memory API`（FFM API，JDK 22 正式）？它取代了之前的什么 API？（JNI、Unsafe）`MemorySegment` 和 `Arena` 分别是什么？FFM API 如何直接调用 C 语言的 `malloc`/`free` 和系统调用？相比 JNI 的性能优势体现在哪里？

184. Java 中 `StackTraceElement` 和 `Throwable.getStackTrace()` 的性能开销有多大？为什么说"频繁创建异常对象"会导致性能问题？异常对象在堆上占用多少字节？`OmitStackTraceInFastThrow`（-XX:-OmitStackTraceInFastThrow）这个 JVM 参数是做什么的？什么场景下 JVM 会把异常"编译为预分配实例"？

185. Java 中的 `MethodHandle` 和 `反射（Reflection）` 有什么区别？`MethodHandle` 的 `invokeExact` 和 `invoke` 有什么不同？`MethodHandles.Lookup` 的权限检查机制是什么？`invokedynamic` 字节码指令和 MethodHandle 的关系？Lambda 表达式底层是怎么用到 `invokedynamic` 的？

---

## Java 并发（6题）

186. 什么是 Java 21 的虚拟线程（Virtual Threads / Project Loom）？虚拟线程和平台线程（Platform Thread）的本质区别是什么？虚拟线程的"载体线程"（Carrier Thread）是什么？什么情况下虚拟线程会被"钉住"（Pinning）无法卸载？如何避免 Pinning？

187. 什么是 Java 21 的结构化并发（Structured Concurrency，预览特性）？`StructuredTaskScope` 是怎么工作的？它解决了什么问题？（任务取消传播、异常聚合、防止线程泄漏）和传统的 ExecutorService 相比有什么优势？`ShutdownOnFailure` 和 `ShutdownOnSuccess` 策略的区别？

188. Java 中 `ThreadGroup` 为什么被标记为废弃（deprecated since JDK 19）？它原本设计用来做什么？为什么实际开发中几乎没人用？线程组内部的"活跃线程数"统计准确吗？用什么替代 ThreadGroup 来管理线程生命周期和异常处理？

189. 什么是 Java 的 `ForkJoinPool` 的 Work-Stealing（工作窃取）算法？每个线程的双端队列（Deque）是怎么工作的？窃取方向为什么是"从尾部窃取、从头部执行"？ForkJoinPool 和普通 ThreadPoolExecutor 的核心区别是什么？什么任务适合 ForkJoin？什么不适合？

190. 什么是 `Phaser`？它和 `CyclicBarrier`、`CountDownLatch` 有什么不同？Phaser 的"动态注册/注销"机制是怎么工作的？（`register()`、`arriveAndDeregister()`）Phaser 的"分层"（Tiering）机制是什么？大量线程同时到达一个 Phaser 时会有性能问题吗？

191. 什么是 Java 的 `ThreadLocalRandom`？它和 `Random`、`SplittableRandom` 有什么区别？`ThreadLocalRandom.current()` 是怎么避免 CAS 竞争的？（每个线程维护自己的种子）在高并发下 `Random.nextInt()` 为什么会成为性能瓶颈？`SplittableRandom` 适合什么场景？

---

## JVM（5题）

192. 什么是 JVM 的 Deoptimization（去优化）？什么情况下 JIT 编译后的代码会被"回退"到解释执行？（类型不匹配、类的继承关系变化等）Deoptimization 的过程是怎样的？OSR 也会被 Deopt 吗？这和"类卸载"有什么关系？

193. 什么是 Java 的 `ClassLoader` 泄漏？在什么场景下自定义 ClassLoader 会导致内存泄漏？（Tomcat 应用热部署、OSGi、动态代理框架）如何排查 ClassLoader 泄漏？（jmap -histo 看类实例数、YourKit/JProfiler 的 ClassLoader 树、Arthas classloader 命令）卸载一个 ClassLoader 需要满足什么条件？

194. 什么是 JVM 的 `Code Cache`？JIT 编译后的 native 代码存在哪里？Code Cache 满了会怎样？`ReservedCodeCacheSize` 和 `InitialCodeCacheSize` 的关系？如何监控 Code Cache 使用率？分层编译 C1/C2 生成的代码分别占用 Code Cache 的比例大概是多少？

195. 什么是 Java 中的 `safepoint`（安全点）？除了 GC 触发 safepoint 外，还有哪些操作需要"Stop-The-World at safepoint"？（偏向锁撤销、类卸载、Deoptimization、Code Cache 清理、JNI critical section）`-XX:+SafepointTimeout` 和 `-XX:SafepointDelay` 的作用是什么？如果某个线程迟迟到不了 safepoint，怎么排查？

196. 什么是 Java 的 `Compact Strings`（JDK 9 引入）？`String` 内部从 `char[]` 改为 `byte[]` + `coder` 后，纯 ASCII 字符串能节省多少内存？`LATIN1` 编码和 `UTF16` 编码的切换时机是什么？这对 `String.compareTo()`、`indexOf()` 等方法有什么影响？

---

## MySQL（5题）

197. MySQL 8.0 的 `原子 DDL`（Atomic DDL）是怎么实现的？InnoDB 的 DDL 操作中，哪些阶段可以回滚、哪些阶段不可以？`ALTER TABLE` 在 MySQL 8.0 中支持 `ALGORITHM=INSTANT` 的场景有哪些？（添加列、修改默认值等）`ALGORITHM=INPLACE` vs `ALGORITHM=COPY` 的区别？Online DDL 和 Copy 算法对锁的影响？

198. 什么是 MySQL 的 `binlog` 三种格式（STATEMENT / ROW / MIXED）？ROW 格式的 binlog 为什么是"最安全"但也最占空间的？`binlog_row_image` 参数（FULL / MINIMAL / NOBLOB）的作用是什么？基于 binlog 的数据同步（Canal / Debezium）在什么场景下会丢数据或重复消费？GTID（全局事务标识）的工作原理是什么？

199. 什么是 MySQL 的 `Buffer Pool` 的预读机制（Read-Ahead）？`innodb_read_ahead_threshold` 参数的含义？什么是"线性预读"（Linear Read-Ahead）和"随机预读"（Random Read-Ahead）？预读的页面会被标记为什么？（`young` 还是 `old`）`innodb_old_blocks_time` 参数和 Buffer Pool 的 LRU 优化有什么关系？

200. MySQL 中 `SHOW ENGINE INNODB STATUS` 的输出中，`TRANSACTIONS` 部分显示了哪些关键信息？`HISTORY LIST LENGTH` 代表什么？这个值过大会导致什么问题？（purge 线程跟不上，undo log 空间膨胀）`PURGE` 线程做了什么？`innodb_purge_threads` 怎么调优？

201. 什么是 MySQL 的 `Generated Column`（生成列 / 计算列）？`VIRTUAL` 和 `STORED` 的区别？生成列上能建索引吗？在什么场景下用生成列比在应用层计算更合适？（JSON 字段提取 + 索引、表达式索引替代方案）生成列和函数索引（Functional Index，MySQL 8.0.13+）有什么异同？

---

## Redis（4题）

202. Redis 的 `EVALSHA` 和 `EVAL` 的区别是什么？`SCRIPT LOAD` + `EVALSHA` 的组合为什么比每次 `EVAL` 更高效？Redis 中的 Lua 脚本有什么执行限制？（超时、无法调用部分命令如 RANDOMKEY、不能返回浮点数等）Redis 7.0 引入的 `Function`（`FUNCTION LOAD` / `FCALL`）和传统 Lua 脚本有什么区别？函数库的管理（`LIBRARY`）怎么用？

203. Redis 的持久化过程中，RDB 快照和 AOF 重写如何协调？`BGSAVE` 和 `BGREWRITEAOF` 能同时执行吗？`server.rdb_child_pid` 和 `server.aof_child_pid` 的互斥关系？Redis 在 fork 子进程时，如果父进程的内存占用很大（比如 50GB），fork 耗时会怎样？`THP`（Transparent Huge Pages）为什么必须关闭？

204. 什么是 Redis 的"懒删除"（Lazy Free）机制？Redis 4.0 引入的 `UNLINK` 命令和 `DEL` 的区别？`FLUSHDB ASYNC` / `FLUSHALL ASYNC` 为什么不会阻塞主线程？`lazyfree-lazy-eviction`、`lazyfree-lazy-expire`、`lazyfree-lazy-server-del` 这几个参数分别控制什么行为？大 Key 删除时 Redis 会阻塞多久？

205. Redis 的 `ACL`（Access Control List，Redis 6.0+）是怎么做权限控制的？相比传统的 `requirepass` 有什么优势？ACL 的命令分类（`+@read`、`+@write`、`+@admin`、`-@dangerous`）有哪些？Redis 的 `USER` 和 `KEY` 两种权限粒度分别是什么？如何用 ACL 实现"某个客户端只能访问以 `user:` 为前缀的 key"？

---

## Spring / Spring Boot（3题）

206. Spring Boot 的 `Actuator` 端点中，`/health`、`/info`、`/metrics`、`/env`、`/beans`、`/mappings` 分别暴露了什么信息？在生产环境中如何安全地暴露这些端点？（`management.endpoints.web.exposure.include`、`management.server.port`）如何自定义 Health Indicator？`/actuator/prometheus` 是怎么把 Micrometer 指标暴露给 Prometheus 的？

207. 什么是 Spring 的 `@Cacheable`、`@CacheEvict`、`@CachePut` 注解？Spring Cache 抽象层和具体缓存实现（Redis / Caffeine / Ehcache）是怎么解耦的？`CacheManager` 和 `@CacheConfig` 的关系？`@Cacheable` 的 `key`、`condition`、`unless`、`sync` 参数分别什么意思？`sync=true` 能解决缓存击穿吗？有什么限制？

208. Spring Boot 3 中 `Observability`（可观测性）是如何整合的？Micrometer 的 `@Timed`、`@Counted` 注解怎么用？分布式追踪（Distributed Tracing）中，Spring Boot 3 默认集成了哪些框架？（Micrometer Tracing + Zipkin / Brave / OpenTelemetry）如何在 Spring Boot 3 中自动导出 Trace 到 OTLP 格式？

---

## 分布式（2题）

209. 什么是分布式系统的"雪花效应"（Thundering Herd / 惊群效应）？在什么场景下会触发？（大量缓存同时过期、大量客户端同时重试、大量 Worker 同时争抢同一批任务）如何避免？如何区分"惊群"和"缓存雪崩"？`jitter`（随机抖动）策略在重试/过期场景中为什么有效？

210. 什么是分布式系统的"脑裂"（Split-Brain）问题？在什么场景下会发生？（ZooKeeper / Nacos / Redis Sentinel 主从切换）ZooKeeper 如何通过"过半数投票"机制防止脑裂？Redis Sentinel 的 `quorum` 参数和 `down-after-milliseconds` 怎么配合设置才能降低脑裂风险？"双主"（双写双活）场景下脑裂的后果是什么？业务层怎么应对？
