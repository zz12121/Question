# 代码/方案设计题 - 第6天（151-180）

## 并发编程类（8题）

151. 你用 `CompletableFuture` 组合了多个异步调用：
```java
public CompletableFuture<OrderDetail> getOrderDetail(Long orderId) {
    CompletableFuture<Order> orderFuture = 
        CompletableFuture.supplyAsync(() -> orderDao.findById(orderId));
    CompletableFuture<User> userFuture = 
        CompletableFuture.supplyAsync(() -> userDao.findById(1L));
    CompletableFuture<List<Item>> itemsFuture = 
        CompletableFuture.supplyAsync(() -> itemDao.findByOrderId(orderId));

    return CompletableFuture.allOf(orderFuture, userFuture, itemsFuture)
        .thenApply(v -> {
            OrderDetail detail = new OrderDetail();
            detail.setOrder(orderFuture.join());
            detail.setUser(userFuture.join());
            detail.setItems(itemsFuture.join());
            return detail;
        });
}
```
**问题：** `supplyAsync` 默认用的是 `ForkJoinPool.commonPool()`。如果 commonPool 的线程被其他任务占满了，这三个查询会怎样？如何指定自定义线程池？`allOf().thenApply()` 中如果某个 Future 已经异常完成了，调用 `.join()` 会抛出什么？

---

152. 你实现了一个带容量限制的"滑动窗口限流器"：
```java
public class SlidingWindowRateLimiter {
    private final long windowSizeMillis;
    private final int maxRequests;
    private final ConcurrentLinkedDeque<Long> requests = new ConcurrentLinkedDeque<>();

    public SlidingWindowRateLimiter(int maxRequests, long windowSizeMillis) {
        this.maxRequests = maxRequests;
        this.windowSizeMillis = windowSizeMillis;
    }

    public synchronized boolean allowRequest() {
        long now = System.currentTimeMillis();
        long windowStart = now - windowSizeMillis;

        // 清理过期的请求记录
        while (!requests.isEmpty() && requests.peekFirst() < windowStart) {
            requests.pollFirst();
        }

        if (requests.size() >= maxRequests) {
            return false;  // 超过限制
        }
        requests.addLast(now);
        return true;
    }
}
```
**问题：** 用了 `synchronized` 但内部用了 `ConcurrentLinkedDeque`，这合理吗？高并发下这个限流器有什么性能瓶颈？有没有无锁的实现方式？Redis + Lua 怎么做分布式滑动窗口限流？

---

153. 你用 `ReentrantLock` 实现了一个"分段锁"的缓存：
```java
public class SegmentedCache<K, V> {
    private static final int SEGMENTS = 16;
    private final ReentrantLock[] locks = new ReentrantLock[SEGMENTS];
    private final Map<K, V>[] segments = new Map[SEGMENTS];

    public SegmentedCache() {
        for (int i = 0; i < SEGMENTS; i++) {
            locks[i] = new ReentrantLock();
            segments[i] = new HashMap<>();
        }
    }

    private int segmentIndex(K key) {
        return Math.abs(key.hashCode()) % SEGMENTS;
    }

    public V get(K key) {
        int idx = segmentIndex(key);
        locks[idx].lock();
        try {
            return segments[idx].get(key);
        } finally {
            locks[idx].unlock();
        }
    }

    // put 方法类似...
}
```
**问题：** 这个分段锁的设计和 ConcurrentHashMap 的 Segment 有什么不同？`key.hashCode()` 返回负数时 `Math.abs()` 会怎样？（Integer.MIN_VALUE 问题）如果两个 key 的 hashCode % SEGMENTS 相同（哈希冲突），它们会竞争同一个锁吗？

---

154. 你用 `Semaphore` 做数据库连接数控制：
```java
Semaphore dbSemaphore = new Semaphore(10);  // 最多10个并发DB操作

public <T> T executeWithLimit(Supplier<T> operation) throws InterruptedException {
    dbSemaphore.acquire();
    try {
        return operation.get();  // 可能耗时很长甚至死锁
    } finally {
        dbSemaphore.release();
    }
}
```
**问题：** 如果 `operation.get()` 抛出异常或执行超时，`release()` 还会被调用吗？如果有线程获取了信号量后一直不释放（比如死循环），其他线程会怎样？如何给 Semaphore 加上"超时自动释放"的能力？

---

155. 你用 `AtomicLong` 做了一个简单的计数器：
```java
AtomicLong counter = new AtomicLong(0);

// 多线程同时递增
for (int i = 0; i < threadCount; i++) {
    executor.submit(() -> {
        for (int j = 0; j < 1000000; j++) {
            counter.incrementAndGet();
        }
    });
}
```
**问题：** 当线程数很多（比如 100 个）且每个线程频繁调用 `incrementAndGet()` 时，CAS 失败重试会导致 CPU 空转（自旋开销）。有哪些优化思路？（LongAdder、Striped64、分桶计数后汇总）LongAdder 的原理是什么？什么场景下 LongAdder 不适用？

---

156. 你写了一个"阻塞式对象池"：
```java
public class ObjectPool<T> {
    private final BlockingQueue<T> pool;
    private final Supplier<T> creator;

    public ObjectPool(int maxSize, Supplier<T> creator) {
        this.pool = new ArrayBlockingQueue<>(maxSize);
        this.creator = creator;
    }

    public T borrowObject() throws InterruptedException {
        T obj = pool.poll();  // 非阻塞取
        if (obj == null) {
            obj = creator.get();  // 池中没对象就创建新的
        }
        return obj;
    }

    public void returnObject(T obj) {
        if (!pool.offer(obj)) {  // 池满了就丢弃
            // 丢弃逻辑...
        }
    }
}
```
**问题：** 这个池没有大小限制——如果并发很高，`poll()` 返回 null 后每次都创建新对象，可能导致对象数量无限增长。怎么改造成"有界对象池"（最大 N 个对象，超出则阻塞等待）？和 Apache Commons Pool2 的 `GenericObjectPool` 相比还缺什么功能？（空闲回收、有效性校验、泄漏检测等）

---

157. 你用 `CountDownLatch` 做了一个"并行聚合"工具：
```java
public class ParallelAggregator {
    public static <T, R> List<R> parallelMap(List<T> data, Function<T, R> mapper, int threads) 
            throws InterruptedException {
        CountDownLatch latch = new CountDownLatch(data.size());
        List<R> results = Collections.synchronizedList(new ArrayList<>());

        ExecutorService executor = Executors.newFixedThreadPool(threads);
        for (T item : data) {
            executor.submit(() -> {
                try {
                    R result = mapper.apply(item);
                    results.add(result);
                } finally {
                    latch.countDown();
                }
            });
        }

        latch.await();
        executor.shutdown();
        return results;
    }
}

// 使用
parallelMap(items, item -> heavyProcess(item), 4);
```
**问题：** 如果 `data.size()` 很大（比如 100 万），`CountDownLatch(1000000)` 合理吗？线程池只有 4 个线程但提交了 100 万个任务，队列会不会爆？`results.add()` 的顺序和原始数据顺序一样吗？如果要保持顺序怎么办？

---

158. 你用 `volatile boolean flag` 控制一个后台线程停止：
```java
private volatile boolean running = true;

public void start() {
    Thread backgroundThread = new Thread(() -> {
        while (running) {
            doWork();
            try {
                Thread.sleep(1000);
            } catch (InterruptedException e) {
                break;
            }
        }
        cleanup();
    });
    backgroundThread.start();
}

public void stop() {
    running = false;
    backgroundThread.interrupt();  // 如果正在 sleep 中可以快速唤醒
}
```
**问题：** `running = false` 和 `interrupt()` 都调用了，哪个先生效取决于 JVM 线程调度。如果 `doWork()` 是一个长时间计算（没有 sleep/wait），`interrupt()` 能中断它吗？`volatile` 写入对正在执行 `doWork()` 的线程何时可见？有没有更好的停止线程的方式？（两阶段终止模式 Two-Phase Termination）

---

## JVM/性能类（6题）

159. 你用 JMH 做微基准测试比较两种字符串拼接方式：
```java
@Benchmark
@BenchmarkMode(Mode.AverageTime)
@OutputTimeUnit(TimeUnit.NANOSECONDS)
public String testStringConcat(String a, String b) {
    return a + b;
}

@Benchmark
public StringBuilder testStringBuilder(String a, String b) {
    StringBuilder sb = new StringBuilder();
    sb.append(a).append(b);
    return sb;
}
```
**问题：** JMH 测试结果可能显示两者性能几乎一样！为什么？JIT 编译优化把 `a + b` 优化成了 `StringBuilder` 吗？JMH 的 `@Warmup`、`@Measurement`、`@Fork` 参数分别是什么意思？为什么不能用 main 方法里简单 for 循环来做性能测试？

---

160. 你的代码中大量使用了正则表达式：
```java
// 用户输入的关键字搜索
Pattern pattern = Pattern.compile(userInputKeyword);  // userInput 来自用户输入

// 在循环中使用
for (String line : lines) {
    if (pattern.matcher(line).find()) {
        // 匹配成功
    }
}
```
**问题：** 如果用户输入的是 `(a+)+s` 这种精心构造的正则表达式，会发生什么？（ReDoS 正则拒绝服务攻击）为什么会引起指数级回溯？Java 的 `Pattern.compile()` 如何防范？有没有超时机制？

---

161. 你的服务在 Docker 容器中运行，发现 GC 日志里有大量 Full GC：
```
[Full GC (Allocation Failure)...  [PSYoungGen: 2048K->0K(2048K)] 
[ParOldGen: 70000K->68000K(70000K)] 72048K->68000K(74240K), 1.234 secs]
```
**问题：** `ParOldGen: 70000K->68000K` 说明老年代只回收了 2000K（几乎没回收掉什么），这意味着什么？（内存泄漏 / 堆太小 / 大对象直接进老年代）`[PSYoungGen: 2048K->0K]` 说明新生代是空的吗？为什么 Full GC 后 Old Gen 还是接近满的？

---

162. 你用 Java 的 `ManagementFactory` 监控 JVM 指标：
```java
MemoryMXBean memoryBean = ManagementFactory.getMemoryMXBean();
MemoryUsage heapUsage = memoryBean.getHeapMemoryUsage();
long used = heapUsage.getUsed();       // 已用
long committed = heapUsage.getCommitted(); // 已提交
long max = heapUsage.getMax();          // 最大
double usagePercent = (double) used / max * 100;
System.out.println("堆使用率: " + usagePercent + "%");
```
**问题：** `used`、`committed`、`max` 三者的区别是什么？如果 `committed < max`，JVM 会向 OS 申请更多内存吗？如果 `used > committed` 会怎样？这个指标能反映真实的内存压力吗？（需要考虑 GC 回收的延迟）

---

163. 你的系统用了 Netty 做 TCP 通信：
```java
ServerBootstrap bootstrap = new ServerBootstrap();
bootstrap.group(bossGroup, workerGroup)
    .channel(NioServerSocketChannel.class)
    .option(ChannelOption.SO_BACKLOG, 128)
    .childOption(ChannelOption.TCP_NODELAY, true)
    .childOption(ChannelOption.SO_KEEPALIVE, true)
    .childHandler(new ChannelInitializer<SocketChannel>() {
        @Override
        protected initChannel(SocketChannel ch) {
            ch.pipeline()
                .addLast(new LengthFieldBasedFrameDecoder(1024*1024, 0, 4, 0, 4))
                .addLast(new BusinessHandler());
        }
    });
```
**问题：** `SO_BACKLOG` 设为 128 够吗？高并发下连接请求被操作系统排队后如果队列满了会怎样？`LengthFieldBasedFrameDecoder` 的 5 个参数分别代表什么？如果客户端发送了一个超过 1MB 的包会怎样？

---

164. 你发现线上服务出现"CPU 飙高但 GC 不频繁"的现象，用 `top -H -p <pid>` 发现某个线程 CPU 占用 99%：
```
PID USER      PR  NI  VIRT   RES   SHR S  %CPU  %MEM    TIME+ COMMAND
12345 app       20   0  4.2g  3.1g  56m R  99.9  8.0   0:30 java
```
用 `jstack` 导出该线程堆栈后发现线程一直在跑一个方法：
```
"pool-1-thread-3" #23 Runnable
    java.lang.Thread.State: RUNNABLE
        at com.example.DataProcessor.processLine(DataProcessor.java:156)
```
**问题：** RUNNABLE 状态不一定真的在"运行"，也可能是在等 I/O。如何进一步确认是纯计算密集还是 I/O 等待？如果是纯计算密集（比如死循环或复杂正则回溯），除了重启还能怎么做？（Arthas 的 `thread` 命令、`perf`、`async-profiler` 等）

---

## 数据库/MySQL类（6题）

165. 你的 SQL 用了 `LEFT JOIN` 关联多张表：
```sql
SELECT o.id, o.user_id, u.name, p.product_name, p.price
FROM orders o
LEFT JOIN users u ON o.user_id = u.id
LEFT JOIN order_items oi ON o.id = oi.order_id
LEFT JOIN products p ON oi.product_id = p.id
WHERE o.create_time >= '2024-01-01'
AND o.status IN (1, 2, 3)
ORDER BY o.create_time DESC
LIMIT 50;
```
**问题：** 这条 SQL 有几张表参与？`EXPLAIN` 结果可能出现哪些 type 值？如果 orders 表有 1000 万行，order_items 表有 5000 万行，JOIN 过程中临时表的行数可能是多少？如何判断是否需要拆分成多次查询？

166. 你写了这样一条 UPDATE：
```sql
UPDATE user SET status = 0, update_time = NOW() 
WHERE id IN (
    SELECT user_id FROM blacklist WHERE reason = 'fraud' AND create_time > '2024-01-01'
);
```
**问题：** MySQL 5.7 和 MySQL 8.0 执行这条语句的行为有什么不同？MySQL 5.7 可能会报什么错误？（You can't specify target table 'user' for update in FROM clause）怎么改写成兼容版本？

167. 你的批量插入代码：
```java
// MyBatis-Plus 的 saveBatch
userService.saveBatch(userList, 500);  // 每500条一批

// 开启了 rewriteBatchedStatements=true
jdbc:mysql://localhost:3306/db?rewriteBatchedStatements=true&useServerPrepStmts=false
```
**问题：** 开启 `rewriteBatchedStatements=true` 后，底层实际发给 MySQL 的 SQL 是怎样的？`useServerPrepStmts=false` 为什么必须配合设置？如果批量插入的数据中有主键冲突（Duplicate Key），整批数据都会失败还是只跳过冲突的那一条？

168. 你做了分库分表，按 user_id 取模分了 8 张表：
```java
// 订单表：orders_0 ~ orders_7
int tableIndex = userId % 8;
String tableName = "orders_" + tableIndex;

// 但现在要查"所有订单的总金额"
SELECT SUM(amount) FROM orders_?, WHERE create_time BETWEEN ? AND ?
```
**问题：** 跨表查询怎么处理？ShardingSphere-JDBC 的 `BindingTable` 和 `BroadcastTable` 分别适用于什么场景？如果要做跨库事务（用户表在 DB_A，订单表在 DB_B~DB_I），分布式事务怎么选型？（Seata AT 模式 / TCC / Saga）

169. 你的慢 SQL 排查中发现一条查询：
```sql
SELECT COUNT(*) FROM orders 
WHERE create_time BETWEEN '2024-01-01 00:00:00' AND '2024-12-31 23:59:59';
```
**问题：** `COUNT(*)` 在 InnoDB 中为什么比 MyISAM 慢？InnoDB 没有"直接返回行数"的能力，那它怎么计算 COUNT？如果这张表有 5000 万行，`COUNT(*)` 大概需要多长时间？有没有近似统计的替代方案？（`SHOW TABLE STATUS` 的 Rows 字段、信息_schema 的估算值等）

170. 你用了 MySQL 的窗口函数：
```sql
SELECT 
    id,
    user_id,
    amount,
    ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY amount DESC) as rank
FROM orders
WHERE create_time >= '2024-01-01';
```
**问题：** 窗口函数在 MySQL 8.0 中的性能表现如何？和传统"自连接排序"写法相比呢？如果 `PARTITION BY` 的列选择性很差（比如 user_id 只有几个不同值），性能会怎样？窗口函数能用索引加速吗？

---

## Redis/缓存类（5题）

171. 你用 Redisson 实现了分布式可重入锁：
```java
RLock lock = redisson.getLock("myLock");
try {
    lock.lock(30, TimeUnit.SECONDS);  // 自动30秒过期
    doBusiness();
} finally {
    lock.unlock();
}
```
**问题：** Redisson 的可重入锁是怎么实现的？（Hash 结构 + 重入计数）如果业务逻辑执行了 25 秒还没完成，锁会提前释放吗？Redisson 的"看门狗"（Watchdog）自动续期机制是怎么工作的？续期的间隔是多少？

172. 你用 Redis 做了"排行榜 + 缓存"组合方案：
```java
// 用户下单后更新排行榜
redis.zadd("sales:ranking", orderAmount, productId);

// 同时缓存商品详情
redis.setex("product:" + productId, 3600, productJson);
```
**问题：** ZSet 的 `ZADD` 操作时间复杂度是 O(logN)，如果排行榜有 100 万个商品，每次 ZADD 都要调整树结构，QPS 上限大概是多少？如果要支持"实时更新的全局排行榜"（每秒几千次更新 + 数万次读取），纯 Redis 方案够不够？要不要引入本地缓存做二级？

173. 你用 Redis 的 `MULTI/EXEC` 做事务：
```java
Transaction tx = redis.multi();
tx.decr("stock:1001");
tx.hset("order:2001", "productId", "1001");
tx.hset("order:2001", "userId", "5001");
tx.expire("order:2001", 86400);
List<Object> results = tx.exec();
```
**问题：** Redis 的事务和关系数据库的事务有什么本质区别？Redis 事务中的命令如果中间某条执行失败（比如对 String 类型执行 lpush），其他命令还会执行吗？EXEC 返回的结果数组中失败的那条返回什么？WATCH 命令的作用是什么？

174. 你的系统用 Redis 存储用户的"最近浏览商品列表"：
```java
// 商品浏览时加入历史（保留最新50个）
redis.lpush("history:1001", productId);
redis.ltrim("history:1001", 0, 49);  // 只保留前50个

// 查询浏览历史
List<String> history = redis.lrange("history:1001", 0, 19);  // 取前20个展示
```
**问题：** LPUSH + LTRIM 这个组合在高频写入场景下有什么隐患？每次 LTRIM 都会截断 list，频繁的内存重新分配怎么优化？有没有更合适的数据结构来存"有限长度的有序集合"？（考虑用 ZSet 以时间戳作为 score）

175. 你用 Redis Pipeline 批量执行命令：
```java
Pipeline pipeline = redis.pipelined();
for (String key : keys) {
    pipeline.get(key);  // 一次性发送1000个GET
}
List<Object> results = pipeline.syncAndReturnAll();
```
**问题：** Pipeline 里的命令是原子执行的吗？如果 Pipeline 中间 Redis 宕机重启了，已收到的命令会丢失吗？Pipeline 和 Lua 脚本（EVAL）的区别是什么？Pipeline 适合什么场景？Lua 适合什么场景？

---

## Spring/微服务类（5题）

176. 你的 Spring Boot 应用用了 `@Scheduled` 定时任务：
```java
@Scheduled(cron = "0 0 2 * * ?")  // 每天凌晨2点执行
@SchedulerLock(name = "dailyReportTask", lockAtMostFor = "30m")
public void generateDailyReport() {
    // 生成日报，耗时约5分钟
    reportService.generate();
}
```
**问题：** 如果部署了 3 个实例，每个实例都会触发这个定时任务吗？`@SchedulerLock`（ ShedLock 库）是如何保证只有一个实例执行的？它的锁存在哪里？如果持有锁的实例在任务执行中途宕机了，锁多久释放？其他实例怎么接管？

177. 你的微服务之间通过 gRPC 通信：
```java
@GrpcClient("productService")
private ProductServiceGrpc.ProductServiceBlockingStub productStub;

public Product getProduct(Long id) {
    GetProductRequest request = GetProductRequest.newBuilder()
        .setId(id)
        .build();
    GetProductResponse response = productStub.getProduct(request);
    return convert(response);
}
```
**问题：** gRPC 的 `BlockingStub` 是同步阻塞的，如果在非响应式 Spring MVC Controller 中使用，会占用 Servlet 容器的线程吗？gRPC 的 HTTP/2 多路复用相比 HTTP/1.1 的 Keep-Alive 连接池有什么优势？gRPC 的流式调用（server streaming / client streaming / bidirectional streaming）适合什么场景？

178. 你的 Spring Boot 应用的配置管理：
```yaml
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/appdb
    username: ${DB_USER:root}
    password: ${DB_PASSWORD}  # 必须从环境变量注入，不能硬编码
  config:
    import:
      - optional:nacos:${spring.profiles.active}
      - optional:file:./override.yml
```
**问题：** `spring.config.import` 是 Spring Boot 2.4 引入的新特性，它和旧的 `spring.config.location` 有什么区别？`optional:` 前缀表示什么？多个配置源的优先级怎么定？如果 Nacos 配置中心和本地 override.yml 中有相同 key，以谁为准？

179. 你的项目引入了 Spring Security 做认证授权：
```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(authz -> authz
                .requestMatchers("/api/public/**").permitAll()
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                .anyRequest().authenticated()
            )
            .oauth2ResourceServer(oauth2 -> oauth2.jwt(jwt -> {}))
            .sessionManagement(session -> session
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            );
        return http.build();
    }
}
```
**问题：** `SessionCreationPolicy.STATELESS` 表示什么？JWT Token 的校验流程是怎样的？（签名验签 → Claims 解析 → 权限检查）Token 过期了前端该怎么办？Refresh Token 机制怎么设计？如果 Token 被盗用了怎么撤销？（Token 黑名单 / 短有效期 / JWT with JTI）

180. 你的 Spring Cloud 微服务要做接口级别的全链路灰度发布：
```java
@Component
public class GrayFilter implements OncePerRequestFilter {

    @Override
    protected void doFilterInternal(HttpServletRequest req, HttpServletResponse res, FilterChain chain) {
        String grayTag = req.getHeader("X-Gray-Tag");  // 前端传入灰度标签
        if ("v2".equals(grayTag)) {
            GrayContext.setVersion("v2");  // 设置灰度上下文
        }
        chain.doFilter(req, res);
    }

    @Override
    public void destroy() {
        GrayContext.clear();  // 清理 ThreadLocal
    }
}
```
**问题：** `GrayContext` 内部用什么存储灰度标记？（ThreadLocal）如果后续调用经过了 Feign/RestTemplate 远程调用，灰度标记能自动传递到下游服务吗？怎么做才能让下游也感知到灰度标记？（Header 透传 / Interceptor 注入）如果下游用了 Hystrix/Sentinel 做线程池隔离，ThreadLocal 还有效吗？
