# 代码/方案设计题 - 第7天（181-210）

## 并发编程类（8题）

181. 你用 JDK 21 的虚拟线程做了并发 HTTP 请求：
```java
try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {
    List<Future<String>> futures = new ArrayList<>();
    for (String url : urls) {
        futures.add(executor.submit(() -> httpClient.send(
            HttpRequest.newBuilder(URI.create(url)).GET().build(),
            BodyHandlers.ofString()
        ).body()));
    }
    for (Future<String> f : futures) {
        System.out.println(f.get());
    }
}
```
**问题：** 虚拟线程的 `newVirtualThreadPerTaskExecutor()` 和平台线程的 `newFixedThreadPool(200)` 有什么本质区别？如果 `httpClient.send()` 底层用了 `synchronized`（比如 JDK 11 的 HttpClient 内部锁），虚拟线程会发生什么？（Pinning / 钉住）如何检测哪些代码导致了 Pinning？`-Djdk.tracePinnedThreads=short` 怎么用？

---

182. 你用结构化并发处理"并行查询 + 超时控制"：
```java
try (var scope = new StructuredTaskScope.ShutdownOnFailure()) {
    Subtask<Order> orderTask = scope.fork(() -> orderClient.getOrder(orderId));
    Subtask<User> userTask = scope.fork(() -> userClient.getUser(userId));
    Subtask<List<Item>> itemsTask = scope.fork(() -> itemClient.getItems(orderId));

    scope.join();           // 等待所有任务完成
    scope.throwIfFailed();  // 如果有任务失败则抛出异常

    Order order = orderTask.get();
    User user = userTask.get();
    List<Item> items = itemsTask.get();
    return new OrderDetail(order, user, items);
}
```
**问题：** `ShutdownOnFailure` 策略是什么意思？如果 `orderTask` 成功了但 `userTask` 抛了异常，`itemsTask` 会被取消吗？`scope.join()` 没有超时参数，如果某个任务一直不返回怎么办？如何给结构化并发加超时？结构化并发和 `CompletableFuture.allOf()` 的核心区别是什么？

---

183. 你用 `StampedLock` 做了一个"配置中心的本地缓存"：
```java
public class ConfigCache {
    private volatile String configValue;
    private final StampedLock lock = new StampedLock();

    public String getConfig() {
        long stamp = lock.tryOptimisticRead();
        String current = configValue;
        if (!lock.validate(stamp)) {
            stamp = lock.readLock();
            try {
                current = configValue;
            } finally {
                lock.unlockRead(stamp);
            }
        }
        return current;
    }

    public void updateConfig(String newValue) {
        long stamp = lock.writeLock();
        try {
            configValue = newValue;
        } finally {
            lock.unlockWrite(stamp);
        }
    }
}
```
**问题：** `tryOptimisticRead` + `validate` 的模式在什么情况下会"乐观读失败"？（有写锁竞争时）乐观读失败后退化为读锁，性能还有优势吗？`StampedLock` 不支持重入，如果把 `getConfig()` 内部再调一个也用 `StampedLock` 的方法会怎样？（死锁）`StampedLock` 适合什么场景？不适合什么场景？

---

184. 你用 `ForkJoinPool` 做大文件并行处理：
```java
public class FileLineCounter extends RecursiveTask<Long> {
    private final File file;
    private final long start;
    private final long end;
    private static final long THRESHOLD = 100_000;

    @Override
    protected Long compute() {
        if (end - start <= THRESHOLD) {
            // 直接计算这一段
            return countLinesInRange(file, start, end);
        }
        long mid = (start + end) / 2;
        FileLineCounter left = new FileLineCounter(file, start, mid);
        FileLineCounter right = new FileLineCounter(file, mid, end);
        left.fork();
        long rightResult = right.compute();
        long leftResult = left.join();
        return leftResult + rightResult;
    }
}
```
**问题：** `left.fork()` + `right.compute()` + `left.join()` 这种写法为什么不写成 `fork()` + `fork()` + `join()` + `join()`？（减少一个线程等待）`ForkJoinPool.commonPool()` 的默认并行度是多少？如果任务是 I/O 密集型的（读文件），ForkJoinPool 是合适的选型吗？为什么说"使用 RandomAccessFile 做行级分割"本身就有问题？

---

185. 你用 `Phaser` 做了多阶段并行任务：
```java
Phaser phaser = new Phaser(3); // 3个线程参与

new Thread(() -> {
    doPhase1Task();
    phaser.arriveAndAwaitAdvance();  // 等其他线程完成阶段1
    doPhase2Task();
    phaser.arriveAndAwaitAdvance();  // 等其他线程完成阶段2
    doPhase3Task();
    phaser.arriveAndDeregister();    // 完成后注销
}).start();
// ... 类似启动另外2个线程
```
**问题：** `arriveAndAwaitAdvance()` 和 `arrive()` + `awaitAdvance()` 有什么区别？如果线程 B 执行阶段 2 时抛了异常，线程 A 和 C 会永远等下去吗？如何让 Phaser 在某个阶段有异常时"整体终止"？（`onAdvance()` 方法覆写）Phaser 的"分层"机制是什么？大量线程（10000+）同时 `arriveAndAwaitAdvance()` 时性能如何？

---

186. 你用 `ThreadLocalRandom` 做随机 ID 生成：
```java
public String generateRandomId() {
    return String.format("%d-%d-%d",
        ThreadLocalRandom.current().nextInt(1000, 9999),
        ThreadLocalRandom.current().nextInt(1000, 9999),
        ThreadLocalRandom.current().nextInt(1000, 9999)
    );
}
```
**问题：** `ThreadLocalRandom.current()` 每次调用会创建新对象吗？为什么在高并发下比 `new Random().nextInt()` 快得多？`ThreadLocalRandom` 的"种子"是怎么维护的？如果线程池中线程被复用，`ThreadLocalRandom` 的种子会重复吗？`SplittableRandom` 和 `ThreadLocalRandom` 的区别是什么？`SplittableRandom` 为什么"可分裂"？

---

187. 你的系统中有一个"全局报警器"，多个模块可以触发报警，但报警需要"聚合 + 去重"（同一类报警 1 分钟内只发一次）：
```java
public class AlertManager {
    private final Map<String, Long> lastAlertTime = new ConcurrentHashMap<>();

    public void alert(String alertType, String message) {
        long now = System.currentTimeMillis();
        Long lastTime = lastAlertTime.putIfAbsent(alertType, now);
        if (lastTime == null || now - lastTime > 60_000) {
            lastAlertTime.put(alertType, now);
            sendNotification(alertType, message);
        }
    }
}
```
**问题：** 这个实现有"检查-执行"的竞态条件吗？两个线程同时进入 `alert("HIGH_CPU", ...)` 会怎样？`putIfAbsent` + 二次判断的组合是线程安全的吗？这个 Map 会无限增长吗？如何实现自动清理过期的报警记录？（`DelayQueue` / `ScheduledExecutorService` 定期清理 / Caffeine 过期缓存）如果报警需要"限流"（每分钟最多发 10 条不同类型的报警），怎么做？

---

188. 你用 `CyclicBarrier` 做了"多线程批量数据处理"：
```java
CyclicBarrier barrier = new CyclicBarrier(4, () -> {
    // 所有线程到达后，主线程做聚合操作
    mergeAllResults();
});

for (int i = 0; i < 4; i++) {
    executor.submit(() -> {
        List<Data> batch = fetchBatch();  // 每个线程取一批数据
        process(batch);                   // 并行处理
        try {
            barrier.await();              // 等其他线程都处理完
        } catch (Exception e) {
            // 异常处理
        }
    });
}
```
**问题：** `CyclicBarrier` 的构造函数第二个参数（`Runnable barrierAction`）是在哪个线程中执行的？（最后一个到达 barrier 的线程）如果 barrierAction 抛了异常，所有 await 的线程会怎样？`barrier.reset()` 在什么场景下使用？`CyclicBarrier` 和 `CountDownLatch` 最大的区别是什么？（可复用 vs 一次性）`CyclicBarrier` 有 `broken` 状态，怎么检测和处理？

---

## JVM/性能类（6题）

189. 你发现线上服务有"内存缓慢增长"的现象，怀疑是内存泄漏，用 Arthas 在线排查：
```bash
# 查看堆内存使用
dashboard

# 查看大对象 TOP 10
heapdump /tmp/heap.hprof

# 查看某个类的实例数
vmtool --action getInstances --className java.lang.String --limit 10
```
**问题：** Arthas 的 `dashboard` 能看到哪些关键指标？`heapdump` 在线上 4GB 堆的服务上执行会不会影响性能？（会触发 STW）如何"增量 dump"或"只 dump 特定对象"？`vmtool --action getInstances` 返回的是存活对象还是包含不可达对象？如何在不 dump 整个堆的情况下，找到"谁持有了某个对象的引用"？`jmap -histo:live` 的 `live` 参数为什么会导致 Full GC？

---

190. 你的服务需要做"JVM 启动参数优化"，当前的启动命令是：
```bash
java -Xms2g -Xmx2g -XX:+UseG1GC -jar app.jar
```
**问题：** 只设置了 `-Xms2g -Xmx2g`（相等），还有哪些重要参数需要设置？`-XX:MaxGCPauseMillis` 设为多少合适？`-XX:G1HeapRegionSize` 默认是多大？2GB 堆应该用多大的 Region？`-XX:+AlwaysPreTouch` 有什么用？`-XX:+UseStringDeduplication` 能节省多少内存？`-XX:MaxDirectMemorySize` 和 `MetaspaceSize` 需要显式设置吗？

---

191. 你在排查一个"偶现的 30 秒 STW（Stop-The-World）"问题，GC 日志显示不是 GC 导致的：
```
[2024-03-15T10:23:45.123+0800] Safepoint cleanup time: 28.7 seconds
```
**问题：** Safepoint 清理耗时 28 秒是什么意思？哪些操作会触发 safepoint？`-XX:+PrintSafepointStatistics`（JDK 8）或 `-Xlog:safepoint*`（JDK 11+）能输出什么信息？如果某个线程在执行 `long loop without safepoint poll`（如 JIT 编译后的循环中），会导致所有其他线程等它到达 safepoint 吗？`-XX:+UseCountedLoopSafepoints` 是做什么的？

---

192. 你用 `ByteBuffer` 做了大文件读取优化：
```java
FileChannel channel = FileChannel.open(path, StandardOpenOption.READ);
ByteBuffer buffer = ByteBuffer.allocateDirect(8 * 1024 * 1024); // 8MB 直接内存
while (channel.read(buffer) != -1) {
    buffer.flip();
    processBuffer(buffer);
    buffer.clear();
}
```
**问题：** `allocateDirect` 分配的直接内存不在 JVM 堆上，GC 会不会回收它？`DirectByteBuffer` 的"Cleaner"机制是怎么工作的？如果不手动释放，直接内存会泄漏吗？`-XX:MaxDirectMemorySize` 不设置的话默认是多少？`DirectByteBuffer` 在 Netty 中是如何池化的（`PooledByteBufAllocator`）？池化后减少了多少 GC 压力？

---

193. 你的服务刚从 JDK 8 升级到 JDK 17，发现某个方法被 `IncompatibleClassChangeError` 打爆了：
```
java.lang.IncompatibleClassChangeError: class com.example.UserService has interface com.example.Cacheable as super class
```
**问题：** `IncompatibleClassChangeError` 是什么类型的错误？（Linkage Error）什么情况下会抛出？（类定义发生了不兼容的变更、类路径冲突、增量编译残留）`NoSuchMethodError`、`NoClassDefFoundError`、`ClassNotFoundException` 的区别是什么？如何用 `-verbose:class` 和 `-Xlog:class+load=info` 来排查类加载顺序？JPMS（Java Platform Module System）能否从根本上解决这类问题？

---

194. 你用 `Java Mission Control`（JMC）+ `JFR`（Java Flight Recorder）做了线上服务的连续录制：
```java
// 启动时配置
-XX:StartFlightRecording=duration=60s,filename=recording.jfr,settings=profile

// 或运行时启动
jcmd <pid> JFR.start name=myrecording duration=60s filename=recording.jfr
```
**问题：** JFR 的 `settings=profile` 和 `settings=default` 的区别是什么？JFR 对线上服务的性能影响有多大？（官方声称 < 1%）JFR 能捕获哪些事件？（对象分配 / 线程阻塞 / 锁竞争 / I/O / GC / 方法执行时间）如何用 JFR 的"自定义事件"（`@Enabled` + `Event` 类）来记录业务指标？JFR 和 async-profiler 各自的优势场景？

---

## 数据库/MySQL类（6题）

195. 你的 SQL 审核平台发现了一条"危险 SQL"：
```sql
SELECT * FROM orders 
WHERE status = 1 
  AND create_time >= DATE_SUB(NOW(), INTERVAL 7 DAY)
ORDER BY update_time DESC 
LIMIT 1000;
```
**问题：** 这个 SQL 可能有什么性能问题？（`SELECT *`、`ORDER BY update_time` 但 `WHERE` 条件是 `create_time`、无法走覆盖索引、`DATE_SUB(NOW())` 导致索引失效）如果 `orders` 表有索引 `idx_status_create_time(status, create_time)`，这条 SQL 能命中索引吗？`ORDER BY` 和 `WHERE` 用了不同的列，MySQL 如何处理？（filesort）如何改写这条 SQL 让它更高效？

---

196. 你的系统在做"批量更新库存"操作：
```java
// 方案1：逐条 UPDATE
for (OrderItem item : items) {
    jdbcTemplate.update(
        "UPDATE product SET stock = stock - ? WHERE id = ? AND stock >= ?",
        item.getQuantity(), item.getProductId(), item.getQuantity()
    );
}

// 方案2：CASE WHEN 批量更新
String sql = "UPDATE product SET stock = CASE id " +
    items.stream().map(i -> "WHEN " + i.getProductId() + " THEN stock - " + i.getQuantity())
        .collect(Collectors.joining(" ")) +
    " END WHERE id IN (" +
    items.stream().map(i -> String.valueOf(i.getProductId()))
        .collect(Collectors.joining(",")) + ")";
jdbcTemplate.update(sql);
```
**问题：** 方案 1 和方案 2 各有什么优缺点？方案 2 的 SQL 长度可能达到几十 KB，MySQL 有 `max_allowed_packet` 限制，怎么处理？方案 2 的 `CASE WHEN` 是原子操作吗？如果中间某一行更新失败会怎样？有没有第三种方案？（存储过程 / 批量 INSERT ON DUPLICATE KEY UPDATE / 乐观锁版本号 + 批量 CAS）

---

197. 你用了 MySQL 8.0 的 CTE（Common Table Expression）递归查询：
```sql
WITH RECURSIVE org_tree AS (
    -- 基础查询：找到根节点
    SELECT id, name, parent_id, 1 AS level
    FROM department WHERE parent_id IS NULL
    
    UNION ALL
    
    -- 递归查询：找到所有子节点
    SELECT d.id, d.name, d.parent_id, t.level + 1
    FROM department d
    JOIN org_tree t ON d.parent_id = t.id
)
SELECT * FROM org_tree WHERE level <= 5;
```
**问题：** MySQL 8.0 引入 CTE 之前，怎么实现树形结构的递归查询？（自连接 N 次 / 应用层循环查）CTE 递归查询的性能和自连接相比如何？`cte_max_recursion_depth` 参数默认值是多少？如果数据中存在循环引用（A → B → C → A），递归 CTE 会怎样？如何限制递归深度？

---

198. 你的系统有"用户行为日志表"，每天新增 5000 万条记录：
```sql
CREATE TABLE user_behavior_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    action VARCHAR(32) NOT NULL,
    target_id BIGINT,
    action_time DATETIME(3) NOT NULL,
    extra JSON
);
```
**问题：** 这张表半年后会到 90 亿条（约 500GB），单表已经扛不住了。归档策略怎么设计？（按月分表 → 冷数据迁移到列式存储）查询时经常要按 `user_id + action_time` 范围查询，分表后跨表查询怎么处理？如果用 ClickHouse 做分析层，数据同步怎么保证？JSON 字段在这张大表上有什么性能影响？

---

199. 你的系统在迁移数据库时发现"在线 DDL"执行时间很长：
```sql
ALTER TABLE orders ADD COLUMN remark VARCHAR(500) DEFAULT '';
```
**问题：** 在 MySQL 8.0 中，这个 DDL 用的是 `ALGORITHM=INSTANT` 还是 `ALGORITHM=INPLACE`？（`ALGORITHM=INSTANT` 只支持加列到表末尾且无默认值限制）如果加的是有默认值的列呢？`ALGORITHM=INPLACE` 执行期间对 DML 语句有什么影响？如何评估 DDL 的预计执行时间？（`pt-online-schema-change` 的 `--dry-run`）`pt-osc` 的原理是什么？（创建影子表 + 同步数据 + 原子切换表名）

---

200. 你用 `Canal` 监听 MySQL 的 binlog 做数据同步到 ES：
```java
CanalConnector connector = CanalConnectors.newSingleConnector(
    new InetSocketAddress("mysql-host", 11111), "example", "", "");
connector.connect();
connector.subscribe(".*\\..*");
connector.rollback();
while (running) {
    Message message = connector.getWithoutAck(100);
    batchId = message.getId();
    List<CanalEntry.Entry> entries = message.getEntries();
    for (CanalEntry.Entry entry : entries) {
        // 解析 binlog event → 写入 ES
    }
    connector.ack(batchId);
}
```
**问题：** Canal 的 `getWithoutAck(100)` 中参数 100 代表什么？（每次最多拉取 100 条）如果 Canal 消费速度跟不上 binlog 产生速度，binlog 文件会不会堆积？Canal 的 `ack` 机制和 Kafka 的 offset 有什么区别？如果 Canal 进程重启，从哪里恢复消费？Canal 对 MySQL 主库有性能影响吗？（dump 协议 / slave 模拟）

---

## Redis/缓存类（5题）

201. 你用 Redis 做了"分布式限流器"：
```java
public boolean isAllowed(String userId) {
    String key = "rate_limit:" + userId;
    String script = 
        "local current = redis.call('INCR', KEYS[1]) " +
        "if current == 1 then " +
        "    redis.call('EXPIRE', KEYS[1], ARGV[1]) " +
        "end " +
        "return current";
    
    Long count = redis.eval(script, ScriptOutputType.INTEGER, 
        new String[]{key}, "60");
    return count <= 100;  // 每分钟100次
}
```
**问题：** 这个 Lua 脚本有什么问题？（INCR 和 EXPIRE 不是原子的，虽然 Lua 脚本本身是原子的，但网络层面有没有问题？）`current == 1` 时才设过期时间，如果 key 在 EXPIRE 之前被手动删除了呢？`eval` 每次都要传脚本，性能如何？用 `evalsha` 优化后呢？令牌桶算法（Token Bucket）用 Redis 怎么实现？和这个"固定窗口"限流器有什么区别？

---

202. 你用 Redis 做了"布隆过滤器"防止缓存穿透：
```java
// 使用 RedisBloom 模块
redis.executeCommand("BF.ADD", "user_whitelist", userId);

// 查询时先检查布隆过滤器
boolean mightExist = redis.executeCommand("BF.EXISTS", "user_whitelist", userId);
if (!mightExist) {
    return null;  // 一定不存在，直接返回
}
// 可能存在，继续查 DB
```
**问题：** 布隆过滤器的"假阳性率"（False Positive Rate）和哪些参数有关？（位数组大小 / 哈希函数个数 / 已插入元素数量）`BF.RESERVE` 的 `error_rate` 设为 0.001 和 0.01 有什么区别？内存占用差多少？布隆过滤器能删除元素吗？Counting Bloom Filter 和 Scalable Bloom Filter 分别怎么解决这个问题？Guava 的 `BloomFilter` 和 Redis 的 `RedisBloom` 模块各适合什么场景？

---

203. 你用 Redis 的 `SORT` 命令做了排行榜分页查询：
```java
// 获取销售额 TOP 100，分页取第 2 页（每页 20 条）
List<String> topSellers = redis.sort("sales_ranking",
    SortOptions.builder()
        .limit(20, 20)      // OFFSET 20, COUNT 20
        .order(Order.DESC)
        .alpha()
        .build()
);
```
**问题：** Redis 的 `SORT` 命令时间复杂度是 O(N + M*log(M))，如果排行榜有 100 万个元素，每次 `SORT` 都要全量排序吗？`LIMIT` 参数能减少排序开销吗？`SORT` 在 Redis 6.2+ 被标记为废弃了吗？替代方案是什么？（`LRANGE` for ZSet / `OBJECT ENCODING`）如果需要"按多个维度排序"（先按销售额降序，再按好评数降序），Redis 单层 ZSet 够不够用？

---

204. 你用 Redis 做了"简单的消息队列"（List 实现）：
```java
// 生产者
redis.lpush("order_queue", JSON.toJSONString(order));

// 消费者（阻塞拉取）
while (running) {
    List<String> messages = redis.brpop(30, "order_queue");
    if (messages != null) {
        Order order = JSON.parseObject(messages.get(1), Order.class);
        processOrder(order);
    }
}
```
**问题：** 用 `BRPOP` 做消息队列有什么致命缺陷？（消息丢失 / 无 ACK 机制 / 无广播 / 无死信队列）消费者处理消息时崩溃了，消息就丢了，怎么解决？（LPUSH 到处理中队列 + 处理完再 LREM / Redis Stream 的 PENDING 列表）如果有多个消费者竞争同一个队列的消息，`BRPOP` 能保证"公平消费"吗？（能，List 操作是原子的）为什么生产环境不建议用 Redis List 做消息队列？

---

205. 你的系统在重启后发现 Redis 连接池报了大量 `JedisConnectionException: Could not get a resource from the pool`：
```java
JedisPool pool = new JedisPool(poolConfig, "redis-host", 6379, 3000);

// 使用时
try (Jedis jedis = pool.getResource()) {
    jedis.set("key", "value");
}
```
**问题：** 连接池耗尽的可能原因有哪些？（连接泄漏 / Redis 响应慢 / 网络分区 / maxTotal 设置太小）`poolConfig.setMaxTotal()` 和 `setMaxIdle()` 的关系是什么？如果 `maxTotal=50` 但 `maxIdle=10`，高并发时连接池会频繁创建和销毁连接吗？`testOnBorrow=true` 为什么在生产环境不建议开启？`Jedis` vs `Lettuce` 的连接模型有什么根本区别？（BIO vs NIO）Lettuce 的"共享连接"怎么工作的？

---

## Spring/微服务类（5题）

206. 你的 Spring Boot 应用用了 `@Retryable` 做重试：
```java
@Retryable(value = {SQLException.class, TimeoutException.class}, 
           maxAttempts = 3, 
           backoff = @Backoff(delay = 1000, multiplier = 2))
public Order createOrder(OrderRequest request) {
    // 可能抛出 SQLException 或 TimeoutException
    return orderDao.insert(request);
}

@Recover
public Order recover(SQLException e, OrderRequest request) {
    // 重试3次都失败后的兜底逻辑
    return fallbackOrder(request);
}
```
**问题：** `@Retryable` 的 `maxAttempts = 3` 加上 `multiplier = 2`，三次重试的间隔分别是多少？（1s / 2s / 4s，总计 7s）如果方法参数包含了一个不可序列化的对象，`@Recover` 方法签名怎么写？`@Retryable` 能对"异步方法"（`@Async`）生效吗？`@CircuitBreaker`（Resilience4j）和 `@Retryable` 的区别是什么？什么场景下需要"先重试再熔断"？

---

207. 你用 Spring Cloud Gateway 做了动态路由配置：
```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/api/order/**
          filters:
            - StripPrefix=1
            - name: Retry
              args:
                retries: 3
                statuses: BAD_GATEWAY, GATEWAY_TIMEOUT
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 100
                redis-rate-limiter.burstCapacity: 200
```
**问题：** `lb://order-service` 中的 `lb` 是什么意思？（Load Balancer，通过服务发现做负载均衡）`StripPrefix=1` 的作用是什么？Gateway 的"限流"用的是 Redis 令牌桶算法，`replenishRate=100` 和 `burstCapacity=200` 是什么含义？如果 Redis 宕机了，Gateway 限流会怎样？（默认放行 / 可配置 fail-open / fail-close）Gateway 的 Filter 有 Global Filter 和 Gateway Filter，执行顺序怎么定？

---

208. 你的 Spring Boot 3 应用集成了 OpenTelemetry 做分布式追踪：
```java
// pom.xml
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-tracing-bridge-otel</artifactId>
</dependency>

// 配置
management:
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://jaeger:4318/v1/traces
```
**问题：** `sampling.probability=1.0` 意味着 100% 采样，生产环境设为多少合适？（0.01 ~ 0.1，即 1%~10%）采样率太低会导致什么问题？（稀疏请求的 trace 链路不完整）如何做"智能采样"？（只采样错误请求 / 慢请求 / 采样头透传的请求）Span 的 parent-child 关系是怎么在 HTTP Header 中传递的？（`traceparent` / `W3C Trace Context`）跨 gRPC 调用的 trace 上下文怎么透传？

---

209. 你用 Spring 的 `@RabbitListener` 消费消息：
```java
@RabbitListener(queues = "order.created", concurrency = "5-20")
@Transactional
public void handleOrderCreated(OrderCreatedEvent event) {
    // 1. 更新库存
    inventoryService.deduct(event.getProductId(), event.getQuantity());
    // 2. 发送通知
    notificationService.send(event.getUserId(), "订单创建成功");
    // 3. 记录日志
    logService.record(event);
}
```
**问题：** `concurrency = "5-20"` 是什么意思？（初始 5 个消费者，根据消息积压动态扩展到 20 个）如果步骤 1 成功了但步骤 2 抛了异常，`@Transactional` 会回滚步骤 1 吗？（RabbitMQ 的 ACK 在事务提交后才发送）如果消息重复投递（网络抖动导致 ACK 丢失），怎么保证幂等？`@RabbitListener` 默认是自动 ACK 还是手动 ACK？消息确认失败会怎样？（重新入队 / 死信队列）`prefetch` 参数设为多少合适？

---

210. 你的微服务用了 Spring Security + OAuth2 Resource Server 做 API 认证：
```java
@Bean
public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
    http
        .oauth2ResourceServer(oauth2 -> oauth2
            .jwt(jwt -> jwt
                .jwkSetUri("https://auth-server/.well-known/jwks.json")
                .decoder(jwtDecoder())
            )
        )
        .authorizeHttpRequests(authz -> authz
            .requestMatchers("/api/public/**").permitAll()
            .anyRequest().authenticated()
        );
    return http.build();
}

@Bean
public JwtDecoder jwtDecoder() {
    return JwtDecoders.fromIssuerLocation("https://auth-server");
}
```
**问题：** `jwkSetUri` 是做什么的？（获取 JWT 签名公钥，用于验签）`JwtDecoders.fromIssuerLocation` 会自动发现 jwks.json 的地址吗？JWT 的公钥有缓存吗？如果 auth-server 更新了签名密钥（密钥轮换），Resource Server 怎么感知？如果 auth-server 挂了，Resource Server 还能正常验签吗？（本地缓存的公钥）JWT 的 `iss`（issuer）和 `aud`（audience）校验失败会怎样？多租户场景下怎么支持多个 issuer？
