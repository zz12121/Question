# 代码/方案设计题 - 第2天（31-60）

## 并发编程类（8题）

31. 你用 `synchronized` 实现了一个单例模式：
```java
public class Singleton {
    private static Singleton instance;
    public static synchronized Singleton getInstance() {
        if (instance == null) {
            instance = new Singleton();
        }
        return instance;
    }
}
```
**问题：** 高并发下有什么问题？如何优化？

---

32. 你用 `ConcurrentHashMap` 做缓存，并给每个 key 加了过期时间：
```java
ConcurrentHashMap<String, ValueWithExpire> cache = new ConcurrentHashMap<>();

public V get(K key) {
    ValueWithExpire v = cache.get(key);
    if (v != null && v.isExpired()) {
        cache.remove(key);
        return null;
    }
    return v != null ? v.getValue() : null;
}
```
**问题：** 这段代码有什么问题？并发场景下会出现什么异常？

---

33. 你用 `ExecutorService` 提交了一批任务：
```java
ExecutorService executor = Executors.newFixedThreadPool(10);
for (int i = 0; i < 1000; i++) {
    executor.submit(() -> {
        // 处理任务
    });
}
executor.shutdown();
```
**问题：** 如果任务执行过程中抛出异常，会发生什么？如何捕获异常？

---

34. 你用 `ReentrantLock` 实现了一个带超时的锁：
```java
ReentrantLock lock = new ReentrantLock();
public boolean tryLockWithTimeout() throws InterruptedException {
    return lock.tryLock(5, TimeUnit.SECONDS);
}
```
**问题：** 如果获取锁超时返回 false，后续如何处理？如果一直获取不到锁，可能是什么原因？

---

35. 你用 `BlockingQueue` 实现生产者消费者：
```java
BlockingQueue<Task> queue = new LinkedBlockingQueue<>(100);

// 生产者
queue.put(task);

// 消费者
while (true) {
    Task task = queue.take();
    process(task);
}
```
**问题：** 如果消费者处理任务很慢，队列满了会怎样？如果消费者挂了，队列里的任务怎么处理？

---

36. 你用 `AtomicInteger` 实现计数器：
```java
AtomicInteger counter = new AtomicInteger(0);
public void increment() {
    counter.incrementAndGet();
}
```
**问题：** 如果要求"每秒最多 1000 次"，如何改造？如果多线程竞争严重，有什么替代方案？

---

37. 你用 `ThreadLocal` 存储用户信息：
```java
public class UserContext {
    private static final ThreadLocal<User> currentUser = new ThreadLocal<>();
    
    public static void set(User user) {
        currentUser.set(user);
    }
    
    public static User get() {
        return currentUser.get();
    }
}
```
**问题：** 如果在线程池中使用，会出现什么问题？如何正确清理？

---

38. 你用 `StampedLock` 优化读写锁：
```java
StampedLock lock = new StampedLock();

public void read() {
    long stamp = lock.readLock();
    try {
        // 读操作
    } finally {
        lock.unlockRead(stamp);
    }
}
```
**问题：** `StampedLock` 有什么特点？什么情况下会死锁？乐观读如何使用？

---

## JVM/性能类（6题）

39. 你写了一个日志工具类，频繁创建大对象：
```java
public void log(String msg) {
    byte[] data = msg.getBytes(StandardCharsets.UTF_8);
    // 写入日志
}
```
**问题：** 高并发下有什么问题？如何优化减少 GC 压力？

---

40. 你用 `System.currentTimeMillis()` 获取时间戳：
```java
for (int i = 0; i < 1000000; i++) {
    long time = System.currentTimeMillis();
    // 使用 time
}
```
**问题：** 频繁调用有什么问题？有什么替代方案？

---

41. 你用 `BigDecimal` 进行金额计算：
```java
BigDecimal a = new BigDecimal(0.1);
BigDecimal b = new BigDecimal(0.2);
System.out.println(a.add(b));
```
**问题：** 输出结果是什么？有什么问题？正确的写法是什么？

---

42. 你用 `Arrays.asList()` 转换数组：
```java
String[] arr = {"a", "b", "c"};
List<String> list = Arrays.asList(arr);
list.add("d");
```
**问题：** 这段代码有什么问题？运行时会发生什么异常？

---

43. 你用 `substring()` 截取字符串：
```java
String str = new String(new char[1000000]);
String sub = str.substring(0, 10);
```
**问题：** JDK 6 和 JDK 7+ 的实现有什么区别？内存方面有什么影响？

---

44. 你用 `try-catch` 捕获所有异常：
```java
try {
    // 业务逻辑
} catch (Exception e) {
    // 忽略异常
}
```
**问题：** 这种做法有什么问题？如何正确记录异常信息？

---

## 数据库/MySQL类（6题）

45. 你的查询 SQL：
```sql
SELECT * FROM orders WHERE user_id = 100 ORDER BY create_time DESC LIMIT 1000000, 10;
```
**问题：** 这条 SQL 有什么问题？如何优化深分页？

---

46. 你的更新 SQL：
```sql
UPDATE account SET balance = balance - 100 WHERE user_id = 100;
```
**问题：** 高并发下会出现什么问题？如何保证余额不为负数？

---

47. 你的批量插入：
```java
for (User user : userList) {
    jdbcTemplate.update("INSERT INTO user (name, age) VALUES (?, ?)", user.getName(), user.getAge());
}
```
**问题：** 如果 userList 有 10 万条，有什么问题？如何优化？

---

48. 你的查询用到了 OR 条件：
```sql
SELECT * FROM orders WHERE user_id = 100 OR status = 1;
```
**问题：** 这条 SQL 索引会怎么用？如何改写提高性能？

---

49. 你的表结构：
```sql
CREATE TABLE logs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    content TEXT,
    create_time DATETIME,
    INDEX idx_time (create_time)
);
```
**问题：** 如果每天产生 1000 万条日志，这张表会出现什么问题？如何优化？

---

50. 你的事务代码：
```java
@Transactional
public void transfer(Long from, Long to, BigDecimal amount) {
    accountDao.deduct(from, amount);
    // 发送消息通知
    kafkaTemplate.send("transfer", message);
    accountDao.add(to, amount);
}
```
**问题：** 这段代码有什么问题？如果 Kafka 发送失败会怎样？

---

## Redis/缓存类（5题）

51. 你的缓存更新代码：
```java
public void updateUser(User user) {
    db.update(user);
    redis.del("user:" + user.getId());
}
```
**问题：** 这段代码有什么问题？如果删除缓存失败会怎样？如何优化？

---

52. 你的缓存读取代码：
```java
public User getUser(Long id) {
    String key = "user:" + id;
    String json = redis.get(key);
    if (json == null) {
        User user = db.query(id);
        redis.setex(key, 3600, JSON.toJSONString(user));
        return user;
    }
    return JSON.parseObject(json, User.class);
}
```
**问题：** 如果并发量很大且缓存未命中，会出现什么问题？如何解决？

---

53. 你用 Redis 做限流：
```java
public boolean allowRequest(String userId) {
    String key = "limit:" + userId;
    Long count = redis.incr(key);
    if (count == 1) {
        redis.expire(key, 60);
    }
    return count <= 100;
}
```
**问题：** 这段代码有什么问题？`incr` 和 `expire` 之间如果挂了会怎样？

---

54. 你用 Redis 做排行榜：
```java
public void addScore(String userId, double score) {
    redis.zadd("rank", score, userId);
}

public Set<String> getTop10() {
    return redis.zrevrange("rank", 0, 9);
}
```
**问题：** 如果排行榜数据量很大（1000 万用户），有什么问题？如何优化？

---

55. 你用 Redis 存储 Session：
```java
redis.setex("session:" + sessionId, 1800, userInfo);
```
**问题：** 如果用户一直在操作，Session 还是 30 分钟过期，如何做到"活跃续期"？

---

## Spring/微服务类（5题）

56. 你的 Controller 代码：
```java
@RestController
public class OrderController {
    @Autowired
    private OrderService orderService;
    
    @PostMapping("/order")
    public Result create(@RequestBody Order order) {
        return orderService.create(order);
    }
}
```
**问题：** 如果 `orderService.create()` 执行很慢，会有什么影响？如何优化接口响应？

---

57. 你的 Service 代码：
```java
@Service
public class OrderService {
    @Transactional
    public void create(Order order) {
        orderDao.save(order);
        // 调用库存服务
        inventoryClient.deduct(order.getProductId());
    }
}
```
**问题：** 这段代码有什么问题？事务边界和远程调用混在一起会有什么后果？

---

58. 你的配置类：
```java
@Configuration
public class RestTemplateConfig {
    @Bean
    public RestTemplate restTemplate() {
        return new RestTemplate();
    }
}
```
**问题：** 这样配置 RestTemplate 有什么问题？如何配置连接池和超时？

---

59. 你的 Feign 调用：
```java
@FeignClient(name = "user-service")
public interface UserClient {
    @GetMapping("/user/{id}")
    User getUser(@PathVariable Long id);
}
```
**问题：** 如果 user-service 挂了，会发生什么？如何配置降级？

---

60. 你的过滤器：
```java
@Component
public class AuthFilter implements Filter {
    @Override
    public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain) {
        // 鉴权逻辑
        HttpServletRequest req = (HttpServletRequest) request;
        String token = req.getHeader("token");
        if (token == null) {
            ((HttpServletResponse) response).setStatus(401);
            return;
        }
        chain.doFilter(request, response);
    }
}
```
**问题：** 这段代码有什么问题？如果鉴权逻辑很慢，会影响所有请求吗？如何优化？
