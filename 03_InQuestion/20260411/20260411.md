# 代码/方案设计题 - 第3天（61-90）

## 并发编程类（8题）

61. 你用 `CountDownLatch` 等待多个任务完成：
```java
CountDownLatch latch = new CountDownLatch(3);
for (int i = 0; i < 3; i++) {
    new Thread(() -> {
        doWork();
        latch.countDown();
    }).start();
}
latch.await();
```
**问题：** 如果某个任务执行异常，`countDown()` 没有被调用，会发生什么？如何处理异常？

---

62. 你用 `CyclicBarrier` 实现多线程分阶段执行：
```java
CyclicBarrier barrier = new CyclicBarrier(3);
for (int i = 0; i < 3; i++) {
    new Thread(() -> {
        phase1();
        barrier.await();
        phase2();
    }).start();
}
```
**问题：** `CyclicBarrier` 和 `CountDownLatch` 有什么区别？如果某个线程中断，会发生什么？

---

63. 你用 `Semaphore` 控制并发数：
```java
Semaphore semaphore = new Semaphore(10);
public void handleRequest() {
    semaphore.acquire();
    try {
        process();
    } finally {
        semaphore.release();
    }
}
```
**问题：** 如果 `release()` 调用次数多于 `acquire()`，会发生什么？如何防止？

---

64. 你用 `Future` 获取异步任务结果：
```java
ExecutorService executor = Executors.newSingleThreadExecutor();
Future<Result> future = executor.submit(() -> callRemote());
Result result = future.get();
```
**问题：** `get()` 方法有什么问题？如何设置超时？超时后如何取消任务？

---

65. 你用 `CompletableFuture` 链式调用：
```java
CompletableFuture.supplyAsync(() -> getUser())
    .thenApply(user -> getOrders(user))
    .thenAccept(orders -> process(orders));
```
**问题：** 如果 `getUser()` 抛出异常，后续操作还会执行吗？如何捕获异常并处理？

---

66. 你用 `ConcurrentLinkedQueue` 做无界队列：
```java
ConcurrentLinkedQueue<Task> queue = new ConcurrentLinkedQueue<>();

// 生产者
queue.offer(task);

// 消费者
while (true) {
    Task task = queue.poll();
    if (task != null) process(task);
}
```
**问题：** 如果生产者速度远快于消费者，会出现什么问题？和 `BlockingQueue` 有什么区别？

---

67. 你用 `CopyOnWriteArrayList` 实现读多写少的列表：
```java
CopyOnWriteArrayList<String> list = new CopyOnWriteArrayList<>();

// 读
for (String s : list) {
    System.out.println(s);
}

// 写
list.add("new");
```
**问题：** 写操作的开销大吗？适合什么场景？迭代器有什么特点？

---

68. 你用 `BlockingQueue` 的 `drainTo` 批量消费：
```java
BlockingQueue<Task> queue = new LinkedBlockingQueue<>();
List<Task> batch = new ArrayList<>();
queue.drainTo(batch, 100);
```
**问题：** `drainTo` 有什么优势？如果队列中不足 100 条，会阻塞吗？

---

## JVM/性能类（6题）

69. 你用 `String.intern()` 节省内存：
```java
String s1 = new String("hello").intern();
String s2 = new String("hello").intern();
System.out.println(s1 == s2);
```
**问题：** 输出是什么？`intern()` 的原理是什么？JDK 6 和 JDK 7+ 有什么区别？

---

70. 你用 `Thread.sleep(0)`：
```java
while (running) {
    doWork();
    Thread.sleep(0);
}
```
**问题：** `sleep(0)` 有什么作用？和 `yield()` 有什么区别？

---

71. 你用 `Object.wait()` 和 `notify()`：
```java
synchronized (lock) {
    while (!condition) {
        lock.wait();
    }
    doWork();
}
```
**问题：** 为什么用 `while` 而不是 `if`？`wait()` 会释放锁吗？

---

72. 你用 `Runtime.getRuntime().addShutdownHook()`：
```java
Runtime.getRuntime().addShutdownHook(new Thread(() -> {
    // 清理资源
}));
```
**问题：** ShutdownHook 在什么情况下会执行？什么情况下不会执行？

---

73. 你用 `System.gc()` 主动触发 GC：
```java
System.gc();
```
**问题：** 调用 `System.gc()` 一定会触发 GC 吗？生产环境为什么不建议用？

---

74. 你用 `finalize()` 方法：
```java
protected void finalize() throws Throwable {
    // 清理资源
}
```
**问题：** `finalize()` 有什么问题？为什么不推荐使用？替代方案是什么？

---

## 数据库/MySQL类（6题）

75. 你的批量更新：
```java
for (User user : users) {
    jdbcTemplate.update("UPDATE user SET name = ? WHERE id = ?", user.getName(), user.getId());
}
```
**问题：** 如何优化为批量更新？

---

76. 你的查询用到了函数：
```sql
SELECT * FROM orders WHERE DATE(create_time) = '2024-01-01';
```
**问题：** 这条 SQL 有什么问题？索引会失效吗？如何优化？

---

77. 你的查询用到了隐式类型转换：
```sql
-- phone 是 varchar 类型
SELECT * FROM user WHERE phone = 13800138000;
```
**问题：** 这条 SQL 有什么问题？索引会失效吗？

---

78. 你的查询用到了 `SELECT *`：
```sql
SELECT * FROM orders WHERE user_id = 100;
```
**问题：** 如果表有 100 个字段，有什么问题？如何优化？

---

79. 你的事务隔离级别：
```java
@Transactional(isolation = Isolation.READ_UNCOMMITTED)
public void query() {
    // 查询操作
}
```
**问题：** `READ_UNCOMMITTED` 有什么问题？生产环境一般用什么隔离级别？

---

80. 你的分页查询用到了大偏移量：
```sql
SELECT * FROM orders ORDER BY id LIMIT 5000000, 10;
```
**问题：** 这条 SQL 的执行过程是什么？为什么慢？如何优化？

---

## Redis/缓存类（5题）

81. 你的缓存预热代码：
```java
@PostConstruct
public void init() {
    List<User> users = userDao.findAll();
    for (User user : users) {
        redis.set("user:" + user.getId(), user);
    }
}
```
**问题：** 如果用户量很大（1000 万），有什么问题？如何优化？

---

82. 你的缓存删除代码：
```java
public void deleteUser(Long id) {
    userDao.delete(id);
    redis.del("user:" + id);
}
```
**问题：** 如果删除缓存失败（Redis 挂了），会出现什么问题？如何保证最终一致？

---

83. 你用 Redis 做分布式限流（滑动窗口）：
```java
public boolean allow(String key, int limit, int window) {
    long now = System.currentTimeMillis();
    redis.zadd(key, now, String.valueOf(now));
    redis.zremrangeByScore(key, 0, now - window * 1000);
    Long count = redis.zcard(key);
    redis.expire(key, window);
    return count <= limit;
}
```
**问题：** 这段代码有什么问题？如何保证原子性？

---

84. 你用 Redis 存储热点数据：
```java
public String getHotData(String key) {
    String value = redis.get(key);
    if (value == null) {
        value = db.query(key);
        redis.setex(key, 60, value);
    }
    return value;
}
```
**问题：** 如果热点数据过期瞬间，大量请求同时打到数据库，如何解决？

---

85. 你用 Redis 实现分布式锁（Redisson）：
```java
RLock lock = redissonClient.getLock("myLock");
lock.lock();
try {
    doBusiness();
} finally {
    lock.unlock();
}
```
**问题：** Redisson 的看门狗机制是什么？锁的续期是如何实现的？

---

## Spring/微服务类（5题）

86. 你的 Bean 注入：
```java
@Service
public class OrderService {
    @Autowired
    private UserService userService;
}
```
**问题：** 字段注入有什么问题？推荐用什么注入方式？

---

87. 你的配置属性：
```java
@Value("${server.timeout:5000}")
private int timeout;
```
**问题：** 如果配置项不存在且没有默认值，会发生什么？如何配置必须项？

---

88. 你的定时任务：
```java
@Scheduled(fixedRate = 60000)
public void task() {
    // 执行任务
}
```
**问题：** 如果任务执行时间超过 60 秒，会发生什么？如何避免任务重叠执行？

---

89. 你的异常处理：
```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(Exception.class)
    public Result handle(Exception e) {
        return Result.fail(e.getMessage());
    }
}
```
**问题：** 这样处理异常有什么问题？如何区分业务异常和系统异常？

---

90. 你的接口返回：
```java
@GetMapping("/user/{id}")
public User getUser(@PathVariable Long id) {
    return userService.getById(id);
}
```
**问题：** 如果用户不存在，返回什么？如何统一包装返回结果？
