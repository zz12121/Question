# 代码/方案设计题 - 第5天（121-150）

## 并发编程类（8题）

121. 你用 `HashMap` 在多线程环境下做缓存：
```java
private Map<String, Object> cache = new HashMap<>();

public Object get(String key) {
    if (!cache.containsKey(key)) {
        cache.put(key, loadFromDB(key));
    }
    return cache.get(key);
}
```
**问题：** 多线程下会出现什么问题？JDK 7 和 JDK 8 的 `HashMap` 多线程问题有什么区别？

---

122. 你用线程池处理任务，但希望所有任务完成后再继续：
```java
ExecutorService executor = Executors.newFixedThreadPool(5);
for (Task task : tasks) {
    executor.submit(() -> process(task));
}
executor.shutdown();
executor.awaitTermination(Long.MAX_VALUE, TimeUnit.NANOSECONDS);
```
**问题：** 这段代码有什么问题？如果某个任务抛出异常，`awaitTermination` 还会等待吗？

---

123. 你实现了一个线程安全的懒加载：
```java
private volatile Map<String, String> config;

public Map<String, String> getConfig() {
    if (config == null) {
        synchronized (this) {
            if (config == null) {
                config = loadConfig();
            }
        }
    }
    return config;
}
```
**问题：** `volatile` 修饰 `Map` 有什么作用？如果 `loadConfig()` 抛出异常，会怎样？

---

124. 你用 `InheritableThreadLocal` 传递父子线程数据：
```java
InheritableThreadLocal<String> context = new InheritableThreadLocal<>();
context.set("userId=123");

ExecutorService executor = Executors.newFixedThreadPool(5);
executor.submit(() -> {
    System.out.println(context.get());
});
```
**问题：** 线程池中子线程能正确拿到父线程的数据吗？有什么隐患？

---

125. 你用 `synchronized` 锁住 `this`：
```java
public class Counter {
    private int count = 0;
    
    public synchronized void increment() {
        count++;
    }
    
    public synchronized int getCount() {
        return count;
    }
}
```
**问题：** 如果创建了多个 `Counter` 实例，锁是共享的吗？如何改成全局计数器？

---

126. 你用 `Timer` 做定时任务：
```java
Timer timer = new Timer();
timer.scheduleAtFixedRate(new TimerTask() {
    @Override
    public void run() {
        doTask();
    }
}, 0, 1000);
```
**问题：** `Timer` 有哪些问题？为什么推荐用 `ScheduledExecutorService` 代替？

---

127. 你用 `Callable` 和 `Future` 获取异步结果：
```java
Future<Integer> future = executor.submit(() -> {
    Thread.sleep(10000);
    return 42;
});
Integer result = future.get(5, TimeUnit.SECONDS);
```
**问题：** 超时后任务还在执行吗？如何真正取消任务？`cancel(true)` 能保证任务停止吗？

---

128. 你用 `CountDownLatch` 实现服务启动等待：
```java
CountDownLatch latch = new CountDownLatch(3);

initDatabase(latch);
initCache(latch);
initMQ(latch);

latch.await();
startServer();
```
**问题：** 如果 `initDatabase` 中发生异常没有调用 `countDown()`，`await()` 会一直等下去吗？如何防止？

---

## JVM/性能类（6题）

129. 你的代码频繁创建对象：
```java
public List<String> process(List<String> input) {
    List<String> result = new ArrayList<>();
    for (String s : input) {
        result.add(s.toUpperCase());
    }
    return result;
}
```
**问题：** JVM 的逃逸分析能优化这段代码吗？什么情况下 `result` 可以栈上分配？

---

130. 你用 `-XX:+PrintGCDetails` 观察 GC 日志，发现频繁 Young GC：
```
[GC (Allocation Failure) [PSYoungGen: 65536K->10240K(76288K)] 65536K->14336K(251392K), 0.0123 secs]
```
**问题：** `Allocation Failure` 是什么意思？如何通过参数调优减少 Young GC 频率？

---

131. 你的服务用了大量 `byte[]` 缓冲区：
```java
byte[] buffer = new byte[1024 * 1024]; // 每次请求分配 1MB
```
**问题：** 高并发下会有什么问题？如何用对象池优化？

---

132. 你发现一个类加载异常：
```
java.lang.ClassNotFoundException: com.example.MyClass
```
**问题：** 可能是什么原因？如何排查类加载问题？`ClassLoader` 的加载顺序是什么？

---

133. 你用 `java.nio.ByteBuffer` 做直接内存：
```java
ByteBuffer buffer = ByteBuffer.allocateDirect(1024 * 1024 * 100); // 100MB
```
**问题：** 直接内存在 JVM 的哪个区域？什么时候会被回收？如何防止直接内存溢出？

---

134. 你的服务在 Docker 容器中运行，设置了 `-Xmx4g`：
```bash
docker run -m 4g -e JAVA_OPTS="-Xmx4g" myapp
```
**问题：** 这样配置有什么问题？JVM 如何感知容器内存限制？

---

## 数据库/MySQL类（6题）

135. 你的查询 SQL：
```sql
SELECT * FROM orders 
WHERE create_time BETWEEN '2024-01-01' AND '2024-12-31'
AND status = 1
AND user_id = 100;
```
**问题：** 联合索引应该如何设计？字段顺序如何确定？

---

136. 你的代码用了乐观锁：
```java
int rows = orderDao.update(
    "UPDATE orders SET status=2, version=version+1 WHERE id=? AND version=?",
    orderId, currentVersion
);
if (rows == 0) {
    throw new OptimisticLockException("并发冲突，请重试");
}
```
**问题：** 乐观锁在高并发下重试风暴问题如何解决？如何平衡重试次数和用户体验？

---

137. 你用了 MyBatis 的动态 SQL：
```xml
<select id="queryOrders" resultType="Order">
    SELECT * FROM orders
    <where>
        <if test="userId != null">AND user_id = #{userId}</if>
        <if test="status != null">AND status = #{status}</if>
    </where>
</select>
```
**问题：** 如果 `userId` 和 `status` 都为 null，SQL 是什么？性能有什么问题？

---

138. 你的分页查询：
```java
Page<Order> page = orderMapper.selectPage(
    new Page<>(pageNum, pageSize),
    new QueryWrapper<Order>().eq("user_id", userId)
);
```
**问题：** MyBatis-Plus 的分页是如何实现的？`COUNT(*)` 查询有什么优化空间？

---

139. 你的 SQL 用了 `IN` 子查询：
```sql
SELECT * FROM orders 
WHERE user_id IN (SELECT id FROM users WHERE vip_level > 3);
```
**问题：** 子查询和 JOIN 哪个性能更好？什么情况下子查询会导致全表扫描？

---

140. 你做了读写分离，写主库读从库：
```java
@Transactional
public void createOrder(Order order) {
    orderDao.save(order);   // 写主库
    // 事务提交后立即查询
    Order saved = orderDao.findById(order.getId());  // 读从库
}
```
**问题：** 这段代码有什么问题？事务内读写分离会导致什么？

---

## Redis/缓存类（5题）

141. 你用 Redis 做消息队列（Stream）：
```java
redis.xadd("stream:orders", "*", "orderId", orderId, "amount", amount);
redis.xreadgroup("GROUP", "consumers", "worker1", "COUNT", "10", "BLOCK", "0", "STREAMS", "stream:orders", ">");
```
**问题：** Redis Stream 和 List 做队列有什么区别？消费失败的消息如何处理？

---

142. 你用 Redis 做布隆过滤器：
```java
redisBloomFilter.add("bloom:users", userId);
boolean exists = redisBloomFilter.contains("bloom:users", userId);
```
**问题：** 布隆过滤器有什么误判问题？误判率如何控制？如何删除一个元素？

---

143. 你用 Redis 做缓存时设置了相同的过期时间：
```java
redis.setex("product:1001", 3600, data1);
redis.setex("product:1002", 3600, data2);
redis.setex("product:1003", 3600, data3);
```
**问题：** 所有缓存同时过期会有什么问题？如何避免缓存雪崩？

---

144. 你用 Redis 的 `EVAL` 执行 Lua 脚本：
```java
String script = "local val = redis.call('get', KEYS[1]) " +
                "if val == ARGV[1] then " +
                "    return redis.call('del', KEYS[1]) " +
                "else return 0 end";
redis.eval(script, 1, lockKey, requestId);
```
**问题：** `EVAL` 执行期间 Redis 是阻塞的吗？如果脚本执行超时会怎样？

---

145. 你用 Redis 存储 JSON 对象：
```java
redis.set("user:1001", JSON.toJSONString(user));
User user = JSON.parseObject(redis.get("user:1001"), User.class);
```
**问题：** 这种方式有什么问题？和用 Redis Hash 存储对象相比，有什么优缺点？

---

## Spring/微服务类（5题）

146. 你的 Spring Boot 项目启动很慢：
```java
@SpringBootApplication
@ComponentScan(basePackages = "com.example")
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```
**问题：** 启动慢可能有哪些原因？如何定位哪个 Bean 初始化耗时？

---

147. 你用 Spring Cache 做缓存：
```java
@Cacheable(value = "users", key = "#id")
public User getUser(Long id) {
    return userDao.findById(id);
}

@CacheEvict(value = "users", key = "#user.id")
public void updateUser(User user) {
    userDao.update(user);
}
```
**问题：** 如果 `getUser` 查到的是 null，会缓存吗？如果缓存了 null，如何处理？

---

148. 你的微服务用了 Spring Cloud Gateway 做路由：
```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: order_route
          uri: lb://order-service
          predicates:
            - Path=/order/**
```
**问题：** 如果 `order-service` 所有实例都挂了，Gateway 会返回什么响应？如何配置降级？

---

149. 你的服务用了 OpenFeign 调用下游接口，设置了超时：
```yaml
feign:
  client:
    config:
      default:
        connectTimeout: 3000
        readTimeout: 5000
```
**问题：** 如果启用了 Hystrix，超时时间以哪个为准？两者的超时配置如何协调？

---

150. 你用 `@EventListener` 监听 Spring 事件：
```java
@EventListener
public void handleOrderCreated(OrderCreatedEvent event) {
    sendNotification(event.getOrder());
}
```
**问题：** 事件监听是同步还是异步的？如果监听器抛出异常，发布者的事务会回滚吗？如何改成异步？
